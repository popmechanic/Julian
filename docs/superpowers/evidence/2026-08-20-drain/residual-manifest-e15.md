# Residual manifest

<!-- derived from: .claude/ultrapowers/run-20260820-e15/report-r1.json, .claude/ultrapowers/run-20260820-e15/report-r2.json -->
<!-- disposition one of: fixed | acked[:<annotation>] | filed:<ref>[ <note>] | waived:<reason> -->

- completenessFindings-f28354e1167a [completenessFindings] TEST-COVERAGE (blocking gap against the plan's core value): `main()` has zero test coverage, so all three new runner notifications are unverified by the suite. The Task-1 seam exports only `StateCorruptError`/`loadStateFrom`/`writeStateTo`/`saveBeatStateTo`; `main` is not exported and no fixture drives it. All 9 tests in tests/server/mail-glance-runner.test.ts exercise state I/O only. Consequently the #14 container-missing notify (both call sites, scripts/mail-glance.ts:244-250 and :263-268), the #15 drop-count notify (:252-258), and the cap-expiry announcement from the beat path (:318-327) are asserted by no test. The lib halves ARE tested (normalizeSentMessages drop count, element-type rejection, activeHoldIds, mergeHolds, parseStateFile migration) — it is specifically the runner wiring that the plan says makes the beat loud that nothing exercises. I verified by code reading that each notify() sits outside any per-item loop (so 'never one per item' holds) and that announceExpiries dedupes to exactly one per beat. — disposition: fixed
- completenessFindings-6a25a4d4dc49 [completenessFindings] CORRECTNESS (real defect, verified by probe): the sent-listing container guard is asymmetric with the thread-listing guard and misreports on a present-but-non-array container. scripts/mail-glance.ts:251 passes `sentRes.messages ?? []`, which only guards null/undefined, while the thread side at :269 correctly uses `Array.isArray(listRes.threads) ? listRes.threads : []`. Probed against the integrated lib: `normalizeSentMessages("abc")` returns `{messages:[],dropped:3}` — so a string container fires the #14 container-missing notify AND a second, false notify claiming 3 sent messages were dropped (one per character). `normalizeSentMessages({items:[]})` and `normalizeSentMessages(42)` throw `TypeError: ... is not iterable`, which propagates to main().catch and aborts the entire beat — immediately after emitting a notification whose text asserts the beat continues ('known-correspondent set NOT built this beat'). Both directions fail safe for 'Never toward a send' (a shrunken/absent known set only moves threads to strangers, and an abort sends nothing), but the 'Loud beats silent: exactly one notify() per beat per class' constraint is violated and the operator is handed a meaningless drop count. Note: the plan's own Step 4 sketch carries this same asymmetry verbatim, so the implementation followed the plan literally; the defect is in the plan as much as the code. Fix is one character of symmetry: `Array.isArray(sentRes.messages) ? sentRes.messages : []`. — disposition: fixed
- completenessFindings-002b22114742 [completenessFindings] UNVERIFIED CLAIM — GLOBAL CONSTRAINT 'TDD: failing test first for each behavior change': not observable on this tree. Each task landed as a single squashed commit carrying tests and implementation together — 1f18a3f (parent 4bce580, the run BASE), c5626c6 and da9a55c (both parented on the wave-1 fold bb76d1c). No red-state commit exists in the history. The tests themselves are strong and would fail against BASE (I confirmed the asserted symbols — `holds`, `activeHoldIds`, `mergeHolds`, `normalizeSentMessages`'s counting shape, the exported runner helpers — are all absent from the BASE files), but the red-then-green ordering is a claim, not evidence. — disposition: acked:process — squashed commits; tests verified discriminating in-run
- completenessFindings-81172b6430d7 [completenessFindings] RESOLVED (was escalated as CANNOT-VERIFY): `saveBeatStateTo` performs the holds union over `Hold[]` on the integrated tree. Task 3's migration landed — HeartbeatState.holds is Hold[] (scripts/lib/mail-glance-lib.ts:140-144), saveBeatStateTo unions via mergeHolds and prunes expired cap-holds by object identity (scripts/mail-glance.ts:158-171), and legacy `held: string[]` migrates to `{kind:'suspicion', heldUtcDay:''}` with no auto-release (parseStateFile:196-200, tested at mail-glance.test.ts:429 and mail-glance-runner.test.ts:44). A file carrying BOTH keys is rejected as corrupt (:184-186, tested at :480). Task 1's runner fixtures were migrated to `holds` as Step 4.5 required. — disposition: acked:verified-in-run
- completenessFindings-4494037cff51 [completenessFindings] RESOLVED (was escalated as CANNOT-VERIFY): all three plan-designated manual checks pass. Run from an isolated HOME and a cwd with no .env (so no key, no network, no touch of ~/.julian): (a) `DRY_RUN=1 bun scripts/mail-glance.ts --definitely-not-a-flag` prints usage and exits 2, both --hold and --hold-cap documented; (b) `DRY_RUN=1 bun scripts/mail-glance.ts` with no key aborts cleanly at exit 2 with '[glance] no AGENTMAIL_API_KEY in env; aborting' — no crash-shaped regression in main(); (c) `DRY_RUN=1 bun scripts/mail-glance.ts --hold-cap msg_test` prints `[dry] would save state: {..."kind":"cap","heldUtcDay":"2026-08-20"}` — today's UTC day; (d) `--hold msg_test2` still parks suspicion with heldUtcDay ''. The isolated HOME was empty afterward, confirming DRY writes nothing. — disposition: acked:verified-in-run
- completenessFindings-24c54a795905 [completenessFindings] RESOLVED (was escalated as CANNOT-VERIFY): Task 2 and Task 3's concurrent edits to the four shared files compose cleanly. The merged parseStateFile returns ok:false for every malformed shape — 'nope', bare strings, missing id, empty id, missing heldUtcDay, wrong-typed heldUtcDay, null, unknown kind, neither-key, both-keys — all asserted (mail-glance.test.ts:459-482). Both notification classes coexist in main() without either being swallowed. Blast radius is provably confined: the only importers of scripts/mail-glance.ts and scripts/lib/mail-glance-lib.ts anywhere in the repo are the two test files, both green. — disposition: acked:verified-in-run
- completenessFindings-bfcc30b1a818 [completenessFindings] RESOLVED (was escalated as CANNOT-VERIFY): the launchd entry point is structurally unchanged. deploy/ is untouched by the whole diff (`git diff BASE HEAD -- deploy/` is empty), the `import.meta.main` guard wraps the pre-existing `main()` (which BASE already had — the change is only the guard replacing a bare `main().catch(...)`), and check (b) above proves a no-arg invocation still walks the beat path to the key check. `--hold`'s functional behavior is byte-identical apart from its console line, which gained a `(suspicion)` suffix — observable in the daemon log but not a behavior change. — disposition: acked:verified-in-run
- completenessFindings-81e45d120e93 [completenessFindings] NOTE (non-blocking, plan step verified): #17's runbook rewrite is complete and honest. docs/mail-heartbeat.md carries both park commands, names the messageId namespace explicitly ('keyed by the thread's LATEST messageId, not the threadId the reply cap counts by'), states hand-editing as the only suspicion release path, and the BASE sentence 'Holds do not expire on their own' is gone, replaced by the cap/suspicion split including the pre-#18 `held: []` migration note. Task 2 Step 4.2's deaf-beat comment correction also landed (scripts/mail-glance.ts:294-302), correctly scoping that check to inner `messages`-key renames now that the container case is covered above it. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- judgmentCalls-0e0705b9fab2 [judgmentCalls] baseline unknown — setup did not report baselinePassed; treat later test results with care (the suite may have been red before any task ran) — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- judgmentCalls-bf9e835c0153 [judgmentCalls] task 3: plan-gap (delivery-blocking, NOT fixed — file outside my scope): scripts/lib/mail-reply-prompt.md line 18 still tells the spawned reply session to run `bun scripts/mail-glance.ts --hold '<latest message_id>'` when a thread hits its 3-per-UTC-day cap. That IS the cap case #18 exists for, but under this change `--hold` now parks SUSPICION, which never auto-releases — so `--hold-cap` has no caller anywhere in the system and the auto-release never fires in production. The file is in neither my FILES nor SIBLING FILES, so I did not touch it rather than write outside my scope during a parallel wave. It needs a one-token edit (`--hold` -> `--hold-cap`) in a follow-up or at merge. Behavior is safe either way: leaving it means holds persist, which is the same as today. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- judgmentCalls-3a9e0509a73f [judgmentCalls] task 3: plan-defect: plan Step 4.3 places cap-hold expiry AND its notification solely 'at save'. Implemented as expiry detected before classify (announce + prune + saveBeatState) with a second prune of the union inside saveBeatStateTo, deduped by a beat-scoped announced-set. Reason the plan text as written is defective: `activeHoldIds` makes the release take effect at CLASSIFY time regardless of whether any save runs, and on an ordinary beat with no new stranger arrivals `saveBeatState` is never called at all — so the plan's placement would either release a thread with no notification (violating the global constraint that only an explicit, notified cap-hold expiry may make a thread eligible) or never persist the prune. Separately, saveBeatStateTo's read-before-write union would resurrect the expired hold straight off disk if expiry were not also applied to the merged list. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- judgmentCalls-70c157f97c07 [judgmentCalls] task 3: API change beyond the stated Produces contract (additive, Task-1 call sites unaffected): saveBeatStateTo's return type is now `{ expired: Hold[] }` instead of `void`, and it takes an optional third parameter `todayUtcDay` (defaults to today) so expiry is testable without clock mocking. Existing two-argument calls still compile. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- judgmentCalls-ae3d3db074e2 [judgmentCalls] task 3: Added a validation rule the plan did not specify: a state file carrying BOTH `held` and `holds` parses as ok:false ('both holds and held are present — ambiguous hold list'). Two disagreeing hold lists cannot be reconciled without a guess and a wrong guess releases a parked thread; corrupt aborts the beat, which is the safe direction. Our own writer never emits both keys. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- judgmentCalls-bad54cf31156 [judgmentCalls] task 3: File-scope overlap with the sibling task by design: SIBLING FILES lists the same four code/test paths this task declares as Modify/Test. Merge conflicts in scripts/mail-glance.ts, scripts/lib/mail-glance-lib.ts, tests/server/mail-glance.test.ts and tests/server/mail-glance-runner.test.ts are expected and were not worked around. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- judgmentCalls-8e76a6fa8503 [judgmentCalls] task 3: Pre-existing, unrelated: tests/server/integration.test.ts has 2 failures (GET /index.html Cache-Control expected 'no-cache', received null). Verified identical at BASE via git stash — not introduced here and not addressed. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- deferredVerification-f41c37d6c0e4 [deferredVerification] The #14/#15 boundary notifications firing against a real AgentMail dialect change, and the live wire type of the `to[]` field that the new element-type strictness now validates (external) — Requires the live AgentMail API and the AGENTMAIL_API_KEY; I deliberately ran every runner check from a cwd with no .env and an isolated HOME so no network call was made. No code in this repo pins AgentMail's wire shape, so whether `to` is genuinely a string array cannot be established from the tree. The failure direction is safe (a smaller known-correspondent set only reduces eligibility, never toward a send) and would be loud via the new drop notification. — disposition: acked:runbook — carried in the end-gate runbook for Marcus
- deferredVerification-6deedd919d3c [deferredVerification] The com.julian.mail-heartbeat launchd job picking up the change in place on its next 30-minute beat, end-to-end (runtime) — Task 4 states the live heartbeat needs no redeploy because launchd runs the repo script in place. deploy/ is untouched and the no-arg entry point is proven to reach the beat path, but an actual scheduled beat with a real key against the real inbox cannot be executed from a read-only review, and doing so would send mail. — disposition: acked:runbook — carried in the end-gate runbook for Marcus
- deferredVerification-52e6a2c92ba3 [deferredVerification] Production migration of the live ~/.julian/mail-heartbeat.json from the legacy `held: string[]` shape to `holds: Hold[]` (runtime) — The migration is proven by test and by code reading (parseStateFile accepts legacy and migrates as suspicion; the first save rewrites the file in the new shape, and nothing ever writes both keys), but the actual on-disk user state file was not read or written — the review ran against a scratch HOME. The first real beat after this lands performs the migration. — disposition: acked:runbook — carried in the end-gate runbook for Marcus
- completenessFindings-8660dffd9c00 [completenessFindings] Verified on the derived tree: heads/wave-1 = bbe430e7d5e6264c998b5f9a002e70fff4bceecf, detached, `git rev-parse HEAD` confirms the same sha, and it equals the recorded merge sha (no mismatch). Both merged heads are ancestors — task 2 dfa44fe9eedf8b5115e0bacc5e0d707ffea2b481 ANCESTOR, task 3 13ebabdbd7104a3f788fbc6f4eb06d9bad37d7e9 ANCESTOR — so ancestryMisses is empty. Task 1's commit (1f18a3f, the runner seam) is not in this wave's mergedShas but is present via base ee8b799, and its declared Create: path tests/server/mail-glance-runner.test.ts exists and is green. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-ac23850f13ea [completenessFindings] Declared acceptance command is fully green: `bun test tests/server/mail-glance.test.ts` = 57 pass / 0 fail, `cd scripts && bun run test` = 84 vitest pass across 7 files + 2 package-manifest pass. `tests/server/mail-glance-runner.test.ts` in isolation = 15 pass / 0 fail. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-90527d0c93b0 [completenessFindings] DISCLOSURE (not caused by this plan): the wider `bun test tests/server` is 246 pass / 2 fail. Both failures are integration.test.ts 'app shell caching' asserting Cache-Control: no-cache on GET / and GET /index.html; they get 404/null because server/server.ts serves the SPA shell from `app/dist/index.html`, a gitignored build artifact absent from the integration worktree (present in the main checkout). The wave diff (ee8b799..HEAD) touches only docs/mail-heartbeat.md, scripts/lib/mail-reply-prompt.md, scripts/mail-glance.ts and tests/server/mail-glance-runner.test.ts — no server code, no fixture. Filed under deferredVerification. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-39ca673ab9c3 [completenessFindings] GAP (untested path, and the safety-critical one): cap-hold expiry has no beat-level test. The plan calls it 'the ONLY change in this plan that can make a thread eligible' and Global Constraints repeat that. `activeHoldIds` is unit-tested in mail-glance.test.ts and `saveBeatStateTo`'s expired-reporting is unit-tested in mail-glance-runner.test.ts, but no test drives `runBeat` with an expired cap-hold to assert (a) the notification text 'cap-hold(s) expired and released: … — threads eligible again' fires, (b) it fires exactly once when both announcement sites (pre-classify and save-time union) see the same expiry, and (c) the thread actually becomes eligible. The six runBeat tests cover containers, drops, and held-stays-ineligible only. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-f6bd9a71527b [completenessFindings] GAP (untested behavior change introduced by THIS diff): `announceExpiries` moved from a module-level `announcedExpiries` Set to a per-beat `ctx.announced` Set, with a new comment asserting 'Per-beat, not per-process: a second beat in the same process must be as loud as the first.' That is a deliberate semantic change and nothing tests it — no test calls `runBeat` twice in one process with the same expiring hold to prove the second beat still announces. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-ff26c8b34d13 [completenessFindings] UNVERIFIED CLAIM: TDD (Global Constraint). Each task landed as a single squashed commit — dfa44fe for task 2, 13ebabd for task 3 — so red-then-green ordering is not recoverable from the history. The tests are present and meaningful, but 'failing test first' cannot be evidenced from this tree. — disposition: acked:process — squashed commits; tests verified discriminating in-run
- completenessFindings-bbeafc278c1c [completenessFindings] UNTESTED PATH (verified by hand instead): `main()`'s argv branch is unreachable from tests because of the `import.meta.main` guard, so `--hold`/`--hold-cap` parsing, `mergeHolds` dedupe on that path, and `writeState`'s DRY gate have no automated coverage. I exercised all three manually: `DRY_RUN=1 bun scripts/mail-glance.ts --hold-cap msg_test` → exit 0, printed `[dry] would save state: … holds:[{id:msg_test,kind:cap,heldUtcDay:2026-08-21}]` and `[glance] held msg_test (cap)`, writing nothing to ~/.julian (heldUtcDay is UTC, correctly a day ahead of local PT — this also proves parseStateFile reads the real production state file). — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-508928823fd6 [completenessFindings] UNTESTED PATH (verified by hand instead): every `abort()` path calls `process.exit()` and is therefore unexercised by the suite. Manual smokes confirm the launchd contract is unchanged: `DRY_RUN=1 bun scripts/mail-glance.ts --definitely-not-a-flag` prints the three-line usage (both flags documented, as Task 3 Step 4.1 requires), notifies, exit 2; a run with AGENTMAIL_API_KEY unset aborts before any network call with the byte-identical 'no AGENTMAIL_API_KEY in env; aborting', exit 2. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-deef1241e688 [completenessFindings] CANNOT-VERIFY item [3] RESOLVED: `--hold-cap` is still a valid flag on the integrated tree. main() accepts exactly `--hold` and `--hold-cap` (scripts/mail-glance.ts), stamps cap-kind with `utcDay()`, suspicion with '', and merges via `mergeHolds`; usage() names both. Task 2's runner reshape did not touch the argv branch — the diff moves it verbatim into the new main(). — disposition: acked:verified-in-run
- completenessFindings-335dc6ce3027 [completenessFindings] CANNOT-VERIFY item [2] RESOLVED (lib half of Task 2's contract): scripts/lib/mail-glance-lib.ts carries `message ${index}: to contains a non-string element` and `… labels contains a non-string element` rejections, and `normalizeSentMessages(raw: unknown[]): { messages: MailMessage[]; dropped: number }`. Both are covered by mail-glance.test.ts (57 green). The runner reads `.messages`/`.dropped` and notifies once per beat on nonzero drops (tested at mail-glance-runner.test.ts:147). — disposition: acked:verified-in-run
- completenessFindings-728cb6dd9682 [completenessFindings] CANNOT-VERIFY item [2] RESOLVED (launchd job unchanged + corrupt state still aborts): `import.meta.main` guard intact; `loadState`/`saveBeatState` still translate StateCorruptError into the same `… at <path> — beat aborted, nothing sent` abort, now with an injectable notifier defaulting to the real one; DRY gating stays in the STATE_PATH-bound wrappers so injectable cores always perform real writes. `runBeat()` with no deps uses production defaults, so `await runBeat()` from main() is the old beat verbatim. — disposition: acked:verified-in-run
- completenessFindings-c19d5699ce69 [completenessFindings] Global Constraint 'never toward a send' holds: the only eligibility-creating path is cap expiry (announced), legacy `held: string[]` migrates as `{kind:'suspicion', heldUtcDay:''}` (parseStateFile, tested), `outlasts`/`mergeHolds` lean toward the longer hold on every tie so suspicion survives a race with a cap, and a both-keys state file is rejected as corrupt. Two runBeat tests pin the negative directly: a held thread never reaches the spawnReply stub, and the same fixture unheld does. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-8b864ad38dd8 [completenessFindings] Global Constraint 'loud beats silent' holds and is now tested end-to-end: the sent-container and thread-container guards are symmetric, the `Array.isArray(...) ? ... : []` (rather than `?? []`) fix means a present-but-non-array container announces AND lets the second guard also speak in the same beat, and mail-glance-runner.test.ts:131 asserts exactly that. Drops notify once per beat, never once per item (tested). — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-f89a0aa48466 [completenessFindings] Global Constraint 'state writes stay atomic' holds: `writeStateTo` still uses the pid-unique `${path}.${process.pid}.tmp` + `renameSync`, `saveBeatStateTo` still re-reads the on-disk copy and unions holds with a forward-only watermark; both are tested (round-trip leaves no .tmp; union keeps a concurrent hold and refuses a backward watermark). — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-736e59c4439c [completenessFindings] Plan Task 2 Step 4.2 satisfied: the deaf-beat comment now states plainly that the container-key rename is covered by the new guard above and that this narrower check covers a renamed inner `messages` key — the over-claim the task was written to correct is gone. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-2ddf346ba5b4 [completenessFindings] Plan Task 3 Step 5 (#17) satisfied and then some: docs/mail-heartbeat.md documents both flags, names the messageId-vs-threadId namespace, states hand-editing as the only suspicion release path, describes the cap/suspicion lifecycle split, and the redirect added the pre-migration case (release from `held`, never add a `holds` key beside it — both-keys is corrupt). scripts/lib/mail-reply-prompt.md's cap-park now correctly calls `--hold-cap` while the decline/suspicion park correctly stays `--hold`. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- deferredVerification-040324d89bd8 [deferredVerification] tests/server/integration.test.ts — 'app shell caching' (GET / and GET /index.html must carry Cache-Control: no-cache) (runtime) — These two tests spawn server/server.ts and require the gitignored build artifact app/dist/index.html, which exists in the main checkout but not in the fresh integration worktree, so the server falls through to its 404 branch. They fail for that reason alone; this wave's diff touches only scripts/mail-glance.ts, tests/server/mail-glance-runner.test.ts, scripts/lib/mail-reply-prompt.md and docs/mail-heartbeat.md — no server file. Re-run after `app/dist` is built, or in the main checkout. — disposition: acked:runbook — carried in the end-gate runbook for Marcus
- deferredVerification-d5339d536c0c [deferredVerification] scripts/mail-glance.ts — a real no-args beat against the live AgentMail inbox (external) — The launchd entry point's full path (network fetch of /messages and /threads, real notify via osascript, real spawn of a reply session) cannot be exercised without AGENTMAIL_API_KEY and a live inbox. I proved the three shell-level behaviors the plan names (usage/abort exit 2, no-key abort exit 2, DRY --hold-cap writing nothing and stamping today's UTC day) but the wire-facing beat itself is unrun. — disposition: acked:runbook — carried in the end-gate runbook for Marcus
