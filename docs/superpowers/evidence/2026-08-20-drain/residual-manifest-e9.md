# Residual manifest

<!-- derived from: .claude/ultrapowers/run-20260820-e9/report-r1.json -->
<!-- disposition one of: fixed | acked[:<annotation>] | filed:<ref>[ <note>] | waived:<reason> -->

- completenessFindings-907a36335f9a [completenessFindings] RESOLVED (checklist [2], suite execution): the given project test command passes on the integrated tree. `cd sync && bun run test` -> 11 files, 189/189 pass (do.test.ts 19 tests, including all 12 new lineage tests AND the 5 pre-existing schema/oversized tests, so the values arm does not disturb the cell arm). `cd scripts && bun run test` -> 6 files, 82/82 pass + 2/2 bun tests. The escalated 'zero readers means zero breakage' claim is confirmed: `git grep SCHEMA_VERSION` and `git grep storeSchemaVersion` over non-node_modules now hit only docs/plans, the intentional tombstone comment at shared/schema.ts:48, the new creation.test.ts assertion, and the stale do.ts comment below. No live consumer remains. — disposition: acked:verified-in-run
- completenessFindings-62f31c8d7b36 [completenessFindings] CONFIRMED GAP (checklist [2], cross-task consistency) — sync/src/do.ts:177: the LINEAGE_KEYS header comment still reads '...activeSessionId is runtime state and deliberately absent, as is storeSchemaVersion, which a migration is allowed to advance.' On the integrated tree `storeSchemaVersion` no longer exists in VALUES_SCHEMA (retired by task 2). The comment now describes a retired field as a live, migration-advanceable value, and directly contradicts the tombstone the same run wrote at shared/schema.ts:48 ('retired 2026-08-20 (#8)'). This is the exact stale-sibling text the task-1 reviewer escalated; it survived integration because sync/src/do.ts is task-1-owned and the retirement landed in task 2. Comment-only, zero functional coupling — non-blocking, but it is a real documentation defect introduced by the wave and should be cut in a follow-up. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-5d584cbe8f23 [completenessFindings] UNMET PLAN CONTRACT (checklist [1], Produces) — sync/src/do.ts:178: Task 1's Interfaces clause promises 'Exported for tests: LINEAGE_KEYS: readonly string[]'. The symbol is exported and its type (`readonly ['ledgerId','parentLedgerId','lineageNote','createdAt','createdBy']`) is assignable to `readonly string[]`, but `git grep LINEAGE_KEYS` shows zero importers anywhere in the repo — the only uses are internal to do.ts (lines 181, 273, 320). The tests hard-code the key names instead. The declared cross-module contract is therefore unexercised: the `export` keyword is currently dead surface. Non-blocking (nothing breaks), but the promised interface is not actually consumed as named. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-a8e3ee12c04c [completenessFindings] UNTESTED PATH (checklist [1], global constraint 1) — the composition of `performCreation` against a GUARDED store is exercised nowhere. scripts/lib/creation.test.ts builds a plain `createStreamStore` (no middleware, no guards); sync/test/do.test.ts:323 tests the guarded DO store but with a hand-written 5-key `setValues`, not the real `performCreation`. So 'creation.ts keeps working' is established by two half-proofs that never meet. I verified it statically instead and it holds: `performCreation` throws unless `ledgerId` is unset, and it passes all six schema values (five lineage + activeSessionId) in one `setValues`, so (a) every lineage key hits `existing === undefined` and passes the willSetValue guard, and (b) no key is absent from the argument, so TinyBase's delete-values-absent-from-setValues semantics never fire the new addWillDelValue/addWillDelValues guards. Low risk, but it is an untested seam. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-838ad9d3eb1c [completenessFindings] SCOPE NOTE (behavior beyond plan, verified safe) — the implementation adds deletion guards the plan never specified: `addWillDelValueCallback` (do.ts:266) and `addWillDelValuesCallback` (do.ts:271), plus merge-path deletion stripping at do.ts:325-327. This is a genuine hardening (an unguarded delete makes the next write look like a first set, laundering an overwrite into two legal steps) and is covered by two new tests. One side effect worth recording: `addWillDelValuesCallback` refuses a WHOLESALE `store.delValues()` outright once any lineage value is set, which also blocks clearing the mutable `activeSessionId` by that route. `activeSessionId` remains settable and individually deletable, and `git grep` finds no caller of `delValues()` outside do.ts/do.test.ts, so nothing regresses — but the plan's 'activeSessionId stays mutable' is now true with that one narrow exception. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-c5c3643fc19f [completenessFindings] ENVIRONMENT, NOT CODE (checklist [2], the `app` third of the claim) — `cd app && bunx vitest run` and `cd broker && bun run test` do not fail, they do not START: app/node_modules and broker/node_modules are absent in this integration worktree (root has 5 entries, sync 90, scripts 48, app and broker zero), so vite/vitest cannot resolve. I did not install (read-only role). Causal isolation done statically instead: app's only `julian-shared/schema` imports are `STORE_PATH` and `createStreamStore` (app/src/lib/store.ts, store.test.ts, tail.test.ts, debug-sync.ts) — no SCHEMA_VERSION/storeSchemaVersion consumer; broker references the schema only as a tsconfig/vitest path alias, with no source import. Plan Task 2 Step 4 and Task 3's gate both remain formally unrun for these two packages. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-85fe4773d16c [completenessFindings] PRE-EXISTING / UNRELATED FAILURE, but the run never looked — there is a fourth suite nobody executes: root package.json defines `test: bun test tests/`, and task 2 edited `tests/shared/jobs-schema.test.ts`. Neither the plan's Task 3 gate (sync, scripts, app, broker) nor the given project test command includes it. I ran it: 308 pass / 2 FAIL, both in tests/server/integration.test.ts ('app shell caching' — `GET /` returns 404 not 200; `GET /index.html` returns cache-control null not 'no-cache'). These are environment, not this plan: app/dist is unbuilt in this worktree (the server has no SPA shell to serve), and integration.test.ts contains zero references to SCHEMA_VERSION or storeSchemaVersion. The straggler task 2 actually touched, tests/shared/jobs-schema.test.ts, passes 3/3 in isolation. Recommend adding `bun test tests/` to the plan's gate list — a suite the wave edited but the gate never runs is a blind spot regardless of this run's outcome. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-8eff1b29653b [completenessFindings] TDD ORDERING UNVERIFIABLE (checklist [1], global constraint 5) — unchanged from the per-task reviewers' position. Task 1 is one squashed commit (3332445) carrying tests and source together; task 2 likewise (91f7183). The red-then-green sequence is not observable from git history on the integrated tree. Note task 2's commit message honestly discloses scope the plan authorized but did not enumerate ('Fixes two stragglers the grep found: shared/schema.test.ts and tests/shared/jobs-schema.test.ts') — the plan's Step 3 explicitly said 'fix any straggler the grep finds', so this is in-scope, not drift. — disposition: acked:process — squashed commits; tests verified discriminating in-run
- completenessFindings-e7a01674bf07 [completenessFindings] VERIFIED, no defect (checklist [1], the moved #flushing reset) — `flushOversized` no longer owns the `#flushing` reset; `flushGuarded` (do.ts:352-358) now runs both halves and clears the flag in a `finally`. Confirmed the only caller of both flushes is `flushGuarded` (do.ts:354-355), itself invoked solely from the queueMicrotask at do.ts:345, so the moved reset has no other consumer. Re-entrancy holds: the two restore transactions in flushLineageBlocked re-fire the didFinishTransaction listener, which returns early on `#flushing`. The `#restoring` exemption is set and cleared in a try/finally (do.ts:408-421), so a schema rejection cannot leave lineage writable. All 5 pre-existing oversized/schema tests stay green. — disposition: acked:verified-in-run
- completenessFindings-940b63ae1097 [completenessFindings] VERIFIED, no defect (the plan's subtlest requirement, global constraint 3) — the two-step bounce is implemented as specified and, unlike the plan's own sketch, correctly types the temp per key: LINEAGE_RESTORE_NUMBER = -1 for the schema-typed-number `createdAt`, LINEAGE_RESTORE_MARKER string otherwise (do.ts:186-192, 415-416). The implementation also improved on the plan by NOT reusing DROPPED_MARKER, which would have claimed a size problem that never happened. Convergence is proven against a real replica, not just the local store: sync/test/do.test.ts:230 and :309 merge `getMergeableContent()` into a fresh `createStreamStore` replica and assert it reads the true value — that is the stamp-tree assertion the plan asked for, done stronger than the planned `JSON.stringify(...).toContain` check. — disposition: acked:verified-in-run
- judgmentCalls-b89493e7e470 [judgmentCalls] task 2: Fixed two files outside the declared FILES scope (shared/schema.test.ts, tests/shared/jobs-schema.test.ts) — this was explicitly required by the task's own Step 3 instruction to grep for and fix stragglers after removing SCHEMA_VERSION, so it's plan-directed rather than an unauthorized divergence, but flagging since it's outside the literal FILES list. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- judgmentCalls-fdbe95056843 [judgmentCalls] task 2: Initially committed work on a stale worktree HEAD (07a5980) instead of the given BASE (18e2571) — caught via the review-package diff size, corrected via reset --hard to BASE + clean patch reapply + recommit before finalizing. Final HEAD (91f7183) is correctly parented; no residual issue, but noting the process deviation for transparency. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- judgmentCalls-80605c21dbe6 [judgmentCalls] task 1: plan-defect: the plan's merge-path guard reads `const incoming = values[key]; if (incoming === undefined) continue; // absent, or a deletion` — which leaves DELETION entirely unguarded on both write paths. That is a complete bypass of the once-ever contract, not a corner case: a foreign socket deletes ledgerId (lands), then writes ledgerId='EVIL' — and because `existing === undefined` the guard reads that as a first set and passes it. I verified this is real with a failing test before fixing it. Closed task-locally in sync/src/do.ts: added addWillDelValueCallback (refuses deleting a set lineage key) and addWillDelValuesCallback (refuses the wholesale delValues while any lineage value is set, since it would take lineage with it), and changed the merge arm to test `key in values` so a present-but-undefined entry is treated as the deletion it is rather than as absence. Two tests cover it (local delete-then-set, and a merge-path deletion). Both are #restoring-exempt like the setValue guard. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- judgmentCalls-83ddd9b5ba6d [judgmentCalls] task 1: Behavior beyond the plan's letter, following from the deletion fix: store.delValues() is now refused WHOLESALE whenever any lineage value is set — so it will no longer clear activeSessionId/storeSchemaVersion either. Nothing in sync/, scripts/, shared/, or server/ calls delValues or delValue today (grepped), so this costs nothing in the current codebase, but it is a broader refusal than the plan described and is worth a second opinion. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- judgmentCalls-b52c48c4f0a7 [judgmentCalls] task 1: plan-defect: the plan specified the restore bounce's temp value as `${DROPPED_MARKER}:lineage-restore`, i.e. '[dropped: cell exceeded 64 KiB]:lineage-restore'. That receipt asserts a size problem that never happened, on a value refused for lineage reasons. Used a dedicated LINEAGE_RESTORE_MARKER = '[lineage-restore: refused an overwrite of an immutable lineage value]' instead (and LINEAGE_RESTORE_NUMBER = -1 for the number-typed createdAt, per the plan). Text-only divergence; the 'lineage-restore' substring is retained so the assertion in my no-bounce test stays meaningful. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- judgmentCalls-b5e8fcc9f620 [judgmentCalls] task 1: Process correction, disclosed in full: this worktree was cut from stale main (07a5980), not from BASE (18e2571) — I did not run the step-1 anchor check before starting, and caught the drift only when the first review packet came back at 207 KB, showing my BASE..HEAD reverting four other tasks' drain merges across broker/, scripts/, memory/, and docs/. Recovered by saving my two files, `git reset --hard 18e2571`, and restoring them; sync/src/do.ts and sync/test/do.test.ts were byte-identical between 07a5980 and 18e2571 (verified with git diff --stat across the two refs, empty), so nothing was lost or silently merged. The reported headSha 3332445 has 18e2571 as its sole parent, and the suite was re-run green after the reset. The discarded pre-reset commit was c2dafd7. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- judgmentCalls-9189b7c31d9c [judgmentCalls] task 1: Refactor touching pre-existing code: flushOversized() no longer resets #flushing in its own finally — that moved to the new flushGuarded(), which runs both flushes in one microtask and owns the flag. flushOversized() also gained an early return when nothing is pending, so a lineage-only drop no longer opens an empty cell transaction. Its stripping logic is otherwise unchanged and all five pre-existing oversized/schema tests stay green. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- judgmentCalls-e2b5b8f74020 [judgmentCalls] task 1: Not covered by this task's tests: the guard is scoped to the DO's own store. A replica that never syncs back keeps its own divergent lineage locally; convergence depends on it merging the DO's content, which the bounce makes authoritative. The merge-path tests assert convergence from both a fresh replica and the offending remote, but there is no multi-hop or hibernation-across-restart test here. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- deferredVerification-6d377832dcb2 [deferredVerification] Plan Task 2 Step 4 / Task 3 gate — `cd app && bunx vitest run` (runtime) — app/node_modules is not installed in the integration worktree, so vite/vitest cannot boot (ERR_MODULE_NOT_FOUND: 'vite'). The suite does not fail, it never starts; this review role is read-only and did not install dependencies. Statically isolated as low risk: app imports only STORE_PATH and createStreamStore from julian-shared/schema, with no consumer of the retired SCHEMA_VERSION / storeSchemaVersion. — disposition: acked:runbook — carried in the end-gate runbook for Marcus
- deferredVerification-c3e1a64f0355 [deferredVerification] Plan Task 3 gate — `cd broker && bun run test` (runtime) — broker/node_modules is absent, so the `vitest` binary is not on PATH (exit 127). The plan asks this specifically to 'confirm no broker consumer' of the shared schema; static confirmation done instead — broker references julian-shared/schema only as a tsconfig and vitest path alias, with no source import of either retired symbol. — disposition: acked:runbook — carried in the end-gate runbook for Marcus
- deferredVerification-bb06f913189e [deferredVerification] Root suite `bun test tests/` (2 failures in tests/server/integration.test.ts, 'app shell caching') (runtime) — The two failures need a built SPA shell at app/dist, which is unbuilt in this worktree (app dependencies are not installed), so the server returns 404 with no cache-control header. Not attributable to this plan: integration.test.ts contains zero references to SCHEMA_VERSION or storeSchemaVersion, and the file task 2 actually edited (tests/shared/jobs-schema.test.ts) passes 3/3. Confirming these green requires an installed-and-built app package. — disposition: acked:runbook — carried in the end-gate runbook for Marcus
- deferredVerification-7b1f24f82de4 [deferredVerification] Plan Task 4 — deploy rider (`cd sync && bunx wrangler deploy`) and its post-deploy confirmation (manual) — The plan gates this on Marcus's word ('The sync worker redeploys on Marcus's word'), and the confirmation step requires a full-house session driving a scratch client to attempt setValue('ledgerId','test-evil') against the live DO and then inspect the export for the corrective rewrite. Human-gated by design and correctly not attempted by the run; no code deliverable is outstanding for it. — disposition: acked:runbook — carried in the end-gate runbook for Marcus
- deferredVerification-f766576a8250 [deferredVerification] Global constraint 1 end-to-end — performCreation against the live guarded DO store (runtime) — No test composes the real performCreation with the guarded store; creation.test.ts uses an unguarded plain store and the DO test uses a hand-written setValues. Verified statically (all six schema values written in one setValues, so every lineage key hits existing===undefined and no absent-key deletion fires the new delete guards), but observing the real ceremony land against a live DO belongs to the Task 4 post-deploy check. — disposition: acked:runbook — carried in the end-gate runbook for Marcus
