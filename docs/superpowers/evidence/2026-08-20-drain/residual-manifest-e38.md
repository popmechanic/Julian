# Residual manifest

<!-- derived from: .claude/ultrapowers/run-20260820-e38/report-r1.json, .claude/ultrapowers/run-20260820-e38/report-r2.json, .claude/ultrapowers/run-20260820-e38/report-r3.json, .claude/ultrapowers/run-20260820-e38/report-r4.json -->
<!-- disposition one of: fixed | acked[:<annotation>] | filed:<ref>[ <note>] | waived:<reason> -->

- completenessFindings-b95e3ded107c [completenessFindings] BLOCKING — global constraint 'Fail toward duplication, never loss' is violated, and this is a REGRESSION vs. the pre-change behavior. `pageLedger` (scripts/ledger-fold.ts:80,99-110) keys its dedupe seen-set on the full wire row `[ts, sub, service, verb, detail, allowed]`. The gate's `/ledger` SELECT (broker/src/governor.ts:505-515) exposes no `rowid`, so two GENUINELY DISTINCT ledger rows written in the same millisecond by the same lease with the same verb/detail/allowed are byte-identical on the wire and collapse to one — silently, with no warning. CONFIRMED by probe against the integrated build: a fake gate holding 8 rows, 3 of them identical at ts=5000000, returned 6 rows with only 1 of the 3. Note the seen-set is global across the whole fetch, not scoped to the page-overlap window, so this fires even on a SINGLE page where no paging occurs at all — the old `fetchLedger` returned `body.entries` verbatim and would have folded all 3. Concrete impact: the routine section's count for a burst of identical same-ms rows reads 1 instead of 3. The implementation is faithful to the approved plan (the plan specifies this key verbatim), so the defect is in the design, not the coding — but the constraint is a GLOBAL one and the integrated tree violates it. A real fix needs row identity on the wire (expose `rowid` in the governor SELECT and key the seen-set on it), which is a broker change beyond this plan's scope. — disposition: fixed
- completenessFindings-fd4c560efd5a [completenessFindings] CONFIRMED (low severity, not reachable from the fold runner) — `/ledger?before=` with an EMPTY value returns a silent empty page instead of a 400 or an uncursored read. broker/src/as/admin.ts:549-553 guards with `beforeRaw !== null`, but `params.get('before')` on `?before=` yields `''`, and `Number('') === 0` is finite, so the 400 branch is skipped and `before = 0` reaches the governor, which runs `WHERE ts < 0` and matches nothing. Verified the coercion directly: `{raw:'', n:0, finite:true}`. The declared wire contract says a non-numeric `before` is a 400; an empty `before` is neither numeric nor rejected. The fold runner never emits this shape (its cursor is always `smallest + 1`), so it is a face wart rather than a fold defect, but any other `/ledger` consumer that builds the query string from an optional variable will get a silently empty ledger. — disposition: fixed
- completenessFindings-211ba96fbd51 [completenessFindings] UNTESTED PATH — the 'pinned page' escape hatch can permanently skip rows. scripts/ledger-fold.ts:117-122 warns and returns early when a full 200-row page shares one millisecond and the cursor cannot advance; main() (line 202-206) then still advances the watermark to the max ts of what WAS returned, which is at or above the pinned ts. Rows left unreachable at that millisecond are therefore skipped on every subsequent run, not merely on this one. No test covers this branch (the runner suite's fixtures never fill a page with a single ts). Requires >200 ledger rows in one millisecond, so the blast radius is narrow, and the plan sanctioned the warning — but the warning text ('folding what was reachable') does not tell the operator that the unreachable rows are being permanently abandoned rather than deferred. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-62f37378e6d2 [completenessFindings] UNTESTED PATH — a row whose `ts` is unreadable is never folded and is re-fetched forever. `NaN <= sinceTs` is false, so such rows always pass the watermark filter in pageLedger (scripts/ledger-fold.ts:92-95), land under the '' key in groupByMonth, and are dropped by main()'s month filter (line 194) after a stderr warning. The watermark reduce (line 202-205) correctly excludes non-finite ts, so the row is re-fetched and re-warned on every future run in perpetuity, and `entries.length` never reaches 0 so the 'Nothing new since watermark' fast path is dead for that ledger. Behavior is plan-specified and non-destructive, but there is no test for it and no escape from the recurring warning. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-1ff66c827193 [completenessFindings] INFORMATIONAL — the task-1 sha quoted in this prompt's mergedShas (b18b54e0fdd7e97b3c2f7c8bce55f9d0b6c93f4a) does not exist as an object in the repository (`git cat-file -t` fails). The authoritative heads/task-1 slot holds b18b54e92b6b790050b30fab5165bf2fe1853bd5, which does exist and IS an ancestor of HEAD. Treated as a prompt transcription artifact, not a dropped task; ancestryMisses is correctly empty. — disposition: acked:verified-in-run
- completenessFindings-4287bef324af [completenessFindings] RESOLVED — the escalated blocking concern about a 'false red' on the append-honesty test was already caught and repaired inside this run by commit 7d11b64. The original fixture (parent-is-a-regular-file) rejected at appendToLedgerFile's recursive mkdir and never reached the readFile try/catch, so a swallowing catch would have passed it. The committed test now spies on `promises.readFile` from 'node:fs' — the same object the runner imports as `fs` — and rejects with EACCES, leaving the write viable. The test passing is itself proof the spy intercepted (an un-intercepted readFile would succeed on the existing file and `rejects.toThrow(/EACCES/)` would fail), and it additionally asserts the prior bytes survive byte-for-byte. The #38 honest-catch criterion is genuinely verified. — disposition: acked:verified-in-run
- completenessFindings-1ac2992718ea [completenessFindings] VERIFIED (all escalated CANNOT-VERIFY items closed on the integrated tree): `cd scripts && bun run test` exits 0 running both halves (vitest 6 files/69 tests, then bun test 2 tests). `cd broker && bun run test` is 24 files/592 tests green, up from the 589 baseline with no regressions — including the pre-existing /ledger tests at admin.test.ts:657,961, which now traverse `gov.entries(limit, undefined)` and prove `undefined` survives the Durable Object RPC boundary. LEDGER_LIMIT (scripts/ledger-fold.ts:24) is 200 and MAX_LIMIT (broker/src/governor.ts:121) is 200, so the server never caps below the request and the `entries.length < LEDGER_LIMIT` short-circuit does not falsely end paging on page one. The runner pages with the lossless `before = smallest + 1` (line 116), not the lossy `before = lastTs`. groupByMonth buckets unreadable timestamps under '' (utcMonthOf returns '' for NaN), which is exactly what main()'s `grouped.get('') ?? []` relies on. memory/adapters/gate-ledger.md documents BOTH row shapes dated 2026-08-20 and deletes the contradicting 'known gap' passage. `.fold-state.json` is not gitignored, so the doc's 'committed beside the month files' claim is viable. `GATE_BREAKGLASS_SECRET` is still read only inside main(), introducing no ambient secret state. The watermark is written only after every month append completes, so a partial failure re-appends rather than drops. All eleven declared Create/Modify paths exist; the dead `fetchLedger` is removed and `getUtcMonth` remains exported and tested. Scripts fold sources typecheck clean; the broker tsc errors are all pre-existing (duplicate jose install, missing cloudflare:test types, LeaseIdentity in lease-auth) and touch none of the changed code. I additionally ran the uncovered second broker suite `bun run test:mcp` (11 tests) — also green. — disposition: acked:verified-in-run
- judgmentCalls-a22f0f42f66a [judgmentCalls] baseline: test suite was already failing before any task ran (scripts: 1 failed | 5 passed (6 test files), 58 tests passed | broker: 24 passed (24 test files), 589 tests passed | baseline status: PARTIAL — broker suite fully passes; scripts has 1 incompatibility (package-manifest.test.ts uses bun:test, incompatible with vitest runner)) — task results inherit a red suite — disposition: acked:inherited-baseline — the #39 red the plan itself fixed
- judgmentCalls-32a6ad38b625 [judgmentCalls] task 2: The overall TEST COMMAND given to this task ((cd scripts && bunx vitest run) && (cd broker && bun run test)) will still exit non-zero in this worktree because scripts/vitest.config.ts (Task 4, sibling, not yet merged here) hasn't landed to exclude package-manifest.test.ts from vitest collection. This is the pre-existing/expected cross-task gap the task text itself calls out as not my gate; it resolves once Task 3/4's changes are integrated. My own two files (lib/ledger-fold.test.ts, and the runner tests once Task 3 lands) are fully green. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- judgmentCalls-55c80136ae2c [judgmentCalls] task 3: Weak test, not a defect in shipped behavior: the plan-verbatim test 'a non-ENOENT read error propagates instead of truncating' passed even against the OLD swallowing catch, because appendToLedgerFile calls fs.mkdir(dirname(path)) first and that throws ENOTDIR before the read is ever attempted. Of the 5 RED failures, none was this test. The catch change (rethrow non-ENOENT) was implemented exactly as specified and is correct, but it is not independently covered by this suite — a future regression that restores the bare catch would still show green here. A test that stubs fs.readFile (or chmods an unreadable existing month file) would actually pin it. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- judgmentCalls-4bfd14046ef2 [judgmentCalls] task 3: Watermark is advanced from the fetched entry set, including rows whose month bucket was skipped as undated ('' bucket). A row with a readable-but-out-of-range ts that survives Number.isFinite but fails new Date() could raise the watermark without ever being written to a month file. In practice groupByMonth's '' bucket only catches NaN-date timestamps and the reduce guards with Number.isFinite, so the overlap is narrow — but the stderr warning is the only trace such a row leaves. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- judgmentCalls-e7fe6992eed9 [judgmentCalls] task 3: The adapter doc still says, further down, 'rows from other months are filtered out rather than rendered under a heading that would misdate them.' That remains literally true per month file (foldEntries filters), but now reads slightly stale next to the new month-routing paragraph, since out-of-month rows are routed to their own file rather than lost. Left as-is to stay inside the task's declared edit. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- deferredVerification-d85f61372278 [deferredVerification] The first live paged fold against the deployed gate (plan Task 6: `cd broker && bunx wrangler deploy`, then `source .env && bun scripts/ledger-fold.ts`) (external) — Requires the real broker Worker deployed to Cloudflare and the live GATE_BREAKGLASS_SECRET from .env. Every test of pageLedger uses an injected fetchImpl against a fixture gate, so the actual /ledger wire round-trip, the month-routing of real rows, and the new `ok` column in a real appended run are unexercised here. Plan Task 6 is explicitly a release task gated on Marcus's word, and no live deploy is permitted inside implementation tasks. — disposition: acked:runbook — carried in the end-gate runbook for Marcus
- deferredVerification-511bd726e9fa [deferredVerification] memory/ledger/.fold-state.json — created, seeded, and committed beside the month files as memory/adapters/gate-ledger.md now claims (manual) — The file does not exist yet (memory/ledger/ holds only 2026-08.md) and is created by the first live fold run. It is not gitignored, so the documented claim is viable, but whether it is actually committed depends on a human step at the deploy. Plan Task 6 Step 2 also requires a human decision — accept a duplicate Aug 13 run block in 2026-08.md, or pre-seat lastFoldedTs to the max ts of that fold read from the live gate — before the watermark is honest. — disposition: acked:runbook — carried in the end-gate runbook for Marcus
- completenessFindings-4cb12ac57e9d [completenessFindings] RESIDUAL SAME-MS LOSS AT THE WATERMARK (scripts/ledger-fold.ts:126 and :229) — the one boundary the redirect did NOT close. The page cursor is now compound (ts, id), but the run-to-run join is still ts-only: pageLedger skips `entry.ts <= sinceTs`, and main() computes `newWatermark = max(finite ts)`. If the newest folded row is (ts=T, id=100) and the gate writes (T, 101) in that same millisecond but after this run's DO read, the next run's `sinceTs = T` skips it forever. This is exactly the failure class the redirect widened the wire to close (its own commit message: 'distinct same-millisecond rows are routine'), left open at the only boundary that carries no id. No test covers it. Plan-CONFORMANT — Task 3's Produces literally specifies `{ lastFoldedTs: number }` and 'only rows with ts > sinceTs' — so this is a plan-level design limit, not an implementation defect; the fix would be `{ lastFoldedTs, lastFoldedId }` with `ts > since || (ts === since && id > sinceId)`. Operationally concrete: plan Task 6 Step 2 tells Marcus to pre-seat .fold-state.json to 'max ts of the Aug 13 fold', which will silently skip any un-folded row sharing that millisecond. — disposition: fixed
- completenessFindings-d79ad02c3d31 [completenessFindings] UNDECLARED CROSS-PACKAGE COUPLING: LEDGER_LIMIT (scripts/ledger-fold.ts:37 = 200) must equal MAX_LIMIT (broker/src/governor.ts:130 = 200). The pager's ONLY 'more pages exist' signal is `body.entries.length < LEDGER_LIMIT`. If the gate's clamp ever drops below 200, page 1 returns fewer rows than requested, the pager returns early, and the fold silently drops everything older — silent loss, the same class being fixed. The two constants live in separate packages with no shared source, no comment linking them, and no test asserting the equality. They match today, so this is latent, not live. — disposition: fixed
- completenessFindings-360a01d921dc [completenessFindings] UNREADABLE-ts ROWS ARE PERMANENTLY RE-FETCHED AND MISCOUNTED (scripts/ledger-fold.ts:126, :213, :234). `NaN <= sinceTs` is false, so such a row never crosses the watermark; `Number.isFinite` excludes it from the new watermark. It therefore warns on stderr and is skipped on EVERY run, forever. A run containing only such rows prints `Rows folded: N` while appending nothing. Defensive-only in practice (the wire's ts is always a number from Date.now()), and no test covers the path. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-ab105b68fab6 [completenessFindings] TDD EVIDENCE NOT OBSERVABLE (global constraint 'run the failing test before the implementation'). Both redirect commits — 67b1183 (gate) and 396f126 (runner) — land tests and implementation in a single commit with no red-run record. I verified independently that the new tests are genuinely discriminating: the straddle test (runner.test.ts:105) fails under the old ts-only+1ms-overlap cursor, and the three-identical-rows test (:93) fails under the old content-keyed dedupe. So the tests are real gates; only the red-before-green ORDERING is unevidenced. — disposition: acked:process — squashed commits; tests verified discriminating in-run
- completenessFindings-76afbbf9387a [completenessFindings] RESOLVED from the CANNOT-VERIFY list — the compound cursor IS consumed end to end. scripts/ledger-fold.ts:104 sends `&before=${cursor.before}&beforeId=${cursor.beforeId}`; :130 dedupes on `seen.has(entry.id)` (a Set<number>), not on a content tuple; :151 pins the next cursor to the oldest row's (ts, id). The 1ms-overlap hack and its 'ledger page pinned at ts=' give-up warning are gone, replaced by a non-advancing-cursor guard at :143. The runner's gateFrom fixture (runner.test.ts:36-57) matches the deployed gate exactly: fixture `e.ts < before || (e.ts === before && e.id < beforeId)` sorted `b.ts-a.ts || b.id-a.id` vs governor.ts:526 `WHERE ts < ? OR (ts = ? AND rowid < ?) ORDER BY ts DESC, rowid DESC` with `SELECT rowid AS id`. Verified against the real source on this tree, not just the fixture. routing.test.ts:113 additionally proves the compound filter through the real authed HTTP path by body content, not by DO poke. — disposition: acked:verified-in-run
- completenessFindings-e2e3076e20d7 [completenessFindings] RESOLVED from the CANNOT-VERIFY list — full broker suite green with the widened row: 24 files / 598 tests pass, including the exact-shape toEqual at governor-leases.test.ts:439 that 67b1183 updated with `id: expect.any(Number)`. I also ran the broker's SECOND, un-gated half `bun run test:mcp` (vitest.node.config.ts, the real-MCP-client harness) — 11 tests pass and it exercises `GET /ledger 200 OK`. Worth noting for the gate: neither the plan's Task 5 nor the project test command runs test:mcp, so that half is outside the declared gate; I ran it and it is green. — disposition: acked:verified-in-run
- completenessFindings-18ce58df505a [completenessFindings] RESOLVED from the CANNOT-VERIFY list — global constraint 'Format change is forward-only; memory/adapters/gate-ledger.md documents both shapes with the change date (2026-08-20)' HOLDS. gate-ledger.md:62-68 carries the dated ok-column note with both shapes coexisting; :82-91 carries the dated operations note (compound cursor + watermark + month routing); :92-104 documents the new `id` wire field, the tie-resolving SQL, the id-keyed dedupe, and the refusal against an un-redirected gate. The stale 'known gap: refusals indistinguishable' and '200-row window / run it often enough' passages are both gone — I grepped for contradictions and found none. — disposition: acked:verified-in-run
- completenessFindings-4265376637d7 [completenessFindings] RESOLVED from the CANNOT-VERIFY list — Task 3's consumed contract, main()'s watermark composition, and the scripts suite. main() (ledger-fold.ts:199-238) reads state, pages, groups by month, appends per month, and only THEN writes the watermark; a throw anywhere in the append loop propagates to the catch and exits 1 WITHOUT advancing state — so 'fail toward duplication, never loss' holds (subject to finding 1). `cd scripts && bunx vitest run` = 72 passed; `cd scripts && bun run test` (both halves, Task 4's deliverable) = 72 vitest + 2 bun:test, exit 0. The now-unused fetchLedger is deleted with no dangling references; getUtcMonth stays exported and tested. — disposition: acked:verified-in-run
- completenessFindings-c3c17c226fa5 [completenessFindings] GOOD, worth recording: pageLedger:120 hard-fails on a row without `id` ('deploy the broker first') rather than guessing a dedupe key. That is the correct fail-toward-duplication choice, but it creates a HARD DEPLOY ORDERING: the fold now refuses to run against the currently-deployed gate until `bunx wrangler deploy` ships 67b1183. Plan Task 6 already orders deploy before fold, and I confirmed the fold is invoked manually only (no launchd/cron/heartbeat wiring references scripts/ledger-fold.ts), so nothing automated breaks in the interim. — disposition: acked:verified-in-run
- completenessFindings-5cccf5d22e28 [completenessFindings] NOT ATTRIBUTABLE TO THIS RUN, recorded for completeness: `bunx tsc --noEmit -p broker/tsconfig.json` reports errors, but every one is pre-existing dependency-type noise (@cloudflare/workers-types vs DOM lib conflicts, missing @types/ws, undici MockAgent shape). Zero errors point at broker/src or the changed files. There is no typecheck script in either package, so typecheck is not part of the project gate; scripts/ has no tsconfig.json at all. — disposition: acked:verified-in-run
- judgmentCalls-1fb239e85645 [judgmentCalls] task 1: plan-defect: adding `id` to LedgerEntry (mandated by the redirect's Produces contract) broke a pre-existing exact-shape `toEqual` assertion in broker/test/governor-leases.test.ts (not in this task's declared FILES scope, not sibling-owned by any parallel task) — 'the ledgered door name is read from the register, never taken from the caller'. Fixed it minimally by adding `id: expect.any(Number)` to the expected object; no behavior assertions were weakened. This was the only broker test broken by the interface change (grep-confirmed no other exact-shape entries() pins exist in the suite). — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- judgmentCalls-142f901014f8 [judgmentCalls] task 1: The memory/adapters/gate-ledger.md paging-description update mentioned near the end of the redirect text belongs to Task 3's redirect (which owns that file and depends on Task 1), not Task 1's — Task 1's FILES list correctly omits it and I left it untouched to avoid colliding with Task 2 (also a parallel owner of that file in wave 1). — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- deferredVerification-81d913392ccf [deferredVerification] Task 6 Step 1 — `cd broker && bunx wrangler deploy` shipping the /ledger compound cursor (manual) — Declared a `release` task, explicitly gated on Marcus's word by the plan and by the house rule that trust-core deploys are witnessed. Nothing in the diff performs a deploy, which is correct; the global constraint 'No live deploys inside implementation tasks' holds. — disposition: acked:runbook — carried in the end-gate runbook for Marcus
- deferredVerification-437444651767 [deferredVerification] Task 6 Step 3 — `source .env && bun scripts/ledger-fold.ts` first paged fold against the live gate (external) — Requires the deployed broker plus GATE_BREAKGLASS_SECRET from .env; the sandbox has neither. Ordering is now HARD, not merely preferred: pageLedger throws 'deploy the broker first' against any gate that serves rows without `id`, so Step 1 must land before Step 3. Also note for the operator: Step 2's advice to pre-seat .fold-state.json to the Aug 13 fold's max ts is exposed to finding 1 — any un-folded row sharing that exact millisecond is skipped permanently. Accepting the duplicate run block instead is the loss-free option. — disposition: acked:runbook — carried in the end-gate runbook for Marcus
- completenessFindings-72e90d7691fe [completenessFindings] BLOCKING: /Users/marcusestes/Websites/Julian/.claude/ultrapowers/run-20260820-e38/heads/ does not exist — no wave-<n> slot is readable, so the detach target cannot be derived and the integrated tree cannot be verified. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-5da1f19cc27a [completenessFindings] BLOCKING: heads slot task-1 is missing for merged task 1 (Governor + admin face — `before` cursor on /ledger) — treated as an ancestry miss. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-caf7ac770c28 [completenessFindings] BLOCKING: heads slot task-3 is missing for merged task 3 (Fold runner — paging, watermark, month routing, honest catch) — treated as an ancestry miss. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-17f4a7ee478e [completenessFindings] Not verified (consequence of the block): the project test command `(cd scripts && bunx vitest run) && (cd broker && bun run test)` was not run, because running it against an unverified tree would produce a result that cannot be attributed to the run's output. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-a9ad1a45aeb8 [completenessFindings] Not verified (consequence of the block): none of the six GLOBAL CONSTRAINTS (append-only ledger files; forward-only format change; per-command secrets; fail-toward-duplication watermark; no live deploys in implementation tasks; TDD test-first) were checked against the integrated tree. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-cea667ec573b [completenessFindings] Context, not authority: the run recorded merge sha 396f12633797f0331039d6c510d3651076f03bd4, but with no heads/ slot to derive from that value cannot be cross-checked, and it was not used as a detach target. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- judgmentCalls-c78ce39cde62 [judgmentCalls] integration ancestry miss (#70): task 1: Governor + admin face — `before` cursor on /ledger reported merged (headSha MISSING — no) is NOT an ancestor of the integration HEAD — silently dropped; the run is BLOCKED, do not merge — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- judgmentCalls-051c61079772 [judgmentCalls] integration ancestry miss (#70): task 3: Fold runner — paging, watermark, month routing, honest catch reported merged (headSha MISSING — no) is NOT an ancestor of the integration HEAD — silently dropped; the run is BLOCKED, do not merge — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- judgmentCalls-972cf9dbaba3 [judgmentCalls] completeness critic could NOT confirm it reviewed the recorded merge HEAD — possible checkout drift (#29); verify the integration tree before merging — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- judgmentCalls-005964ddecf7 [judgmentCalls] suite acceptance did not pass (committed test suite failed) — gate must not Approve — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-41bbab39e759 [completenessFindings] POSITION VERIFIED — heads/ slot `wave-2` = fd6834a68d72b7604a420e031472aa2bd366c63e (highest-numbered wave slot); detached in /Users/marcusestes/Websites/Julian/.claude/worktrees/wf_run-integration; `git rev-parse HEAD` confirms equality. Recorded merge sha matches the derived value exactly. Working tree clean. — disposition: acked:verified-in-run
- completenessFindings-9479becd58e2 [completenessFindings] ANCESTRY — both recorded heads are ancestors of HEAD: task 1 c1e43e02bd061d1bd118a71589e86ca4f31eaa3a (ANCESTOR), task 3 fd6834a68d72b7604a420e031472aa2bd366c63e (ANCESTOR, == HEAD). ancestryMisses is empty. Both heads/ slot files (`task-1`, `task-3`) are present and well-formed. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-51d68af282b8 [completenessFindings] TESTS — `(cd scripts && bunx vitest run)` → 6 files / 81 tests passed. `(cd broker && bun run test)` → 24 files / 599 tests passed. Additionally ran the plan's own Task 5 gate command `cd scripts && bun run test` (both halves: vitest + `bun test package-manifest.test.ts`) → exit 0, 81 vitest + 2 bun:test pass. No regressions anywhere. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-0ef211bb7f3b [completenessFindings] CANNOT-VERIFY [1] RESOLVED (suite green after the addition) — the whole broker suite passes on the integrated tree: 599/599. The `beforeId` malformed-value tests added by c1e43e0 live in broker/test/routing.test.ts and are among them. — disposition: acked:verified-in-run
- completenessFindings-7967bbbbe6e4 [completenessFindings] CANNOT-VERIFY [1] RESOLVED-AS-CORRECT (no failing-first state for the four beforeId tests) — the redirect's premise holds on the tree: broker/src/as/admin.ts:569-573 already contained the `UNSIGNED_INT` guard for `beforeId`, added by 67b1183, and c1e43e0 touched only broker/test/routing.test.ts (+21 lines, no src). The commit message records that the author verified the new test fails when the guard is deleted — a mutation check, which is the right substitute for red-first when covering an existing guard. Not a gap. — disposition: acked:verified-in-run
- completenessFindings-b34baad4063b [completenessFindings] CANNOT-VERIFY [3] PARTIALLY RESOLVED (TDD red-then-green not evidencable) — the run is four commits (67b1183, 396f126, c1e43e0, fd6834a), each pairing tests with implementation, so commit ordering still cannot exhibit a red state. This is a structural limit of squashed worktree history, not an observable defect: every behavior asserted in the plan is covered by a committed test that passes on this tree. Recording as unverified-by-construction, non-blocking. — disposition: acked:process — squashed commits; tests verified discriminating in-run
- completenessFindings-f25ff3b3d805 [completenessFindings] CANNOT-VERIFY [3] RESOLVED (changed Produces contract has no stale callers) — grepped the whole tree for `pageLedger`, `readFoldState`, `writeFoldState`, `foldStatePath`, `nextWatermark`, `LedgerRowWire`, `fetchLedger`. The only source references are scripts/ledger-fold.ts and scripts/ledger-fold.runner.test.ts. The single remaining hit on the old ts-only signature is the plan document itself (see next finding). `fetchLedger` is fully deleted as the plan required. — disposition: acked:verified-in-run
- completenessFindings-1247bab96161 [completenessFindings] CANNOT-VERIFY [3] RESOLVED (live gate honors the compound cursor) — this is NOT only fixture-verified. broker/test/governor.test.ts exercises the real GovernorDO against real sqlite in workerd: `entries: every row carries a unique numeric id, descending with the sort order` and `entries: compound cursor (before + beforeId) pages losslessly through same-ts rows`, the latter seeding a three-row same-ts tie and asserting the cursor lands strictly between tie-b and tie-a. broker/test/routing.test.ts adds two body-content HTTP-face proofs (`compound cursor actually filters, proven by BODY content`, `beforeId is actually threaded through, not silently dropped`). broker/src/governor.ts:520-542 emits `rowid AS id` in all three branches and orders `ts DESC, rowid DESC` in all three. Contract confirmed on both sides. — disposition: acked:verified-in-run
- completenessFindings-c7fa728b52dd [completenessFindings] GAP (moderate, untested path) — `main()` in scripts/ledger-fold.ts:270-330 is entirely uncovered. Nothing tests the orchestration the plan's Task 3 Step 3.4 introduced: per-month `appendToLedgerFile` routing, the undated-row stderr warning, the `Rows folded: N` accounting, and — most importantly — the global constraint 'the watermark advances only after every month append in a run succeeds'. That constraint holds by inspection (`writeFoldState` sits after the append loop, inside a try whose catch exits 1) but is asserted by no test. A future refactor that moves the state write, or wraps a per-month append in its own catch, would violate the plan's most safety-critical global constraint with the suite still green. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-c0456b7e4776 [completenessFindings] GAP (low, untested defensive paths) — two unreadable-`ts` branches in the pager have no coverage. (a) `isAboveWatermark` (scripts/ledger-fold.ts:113) returns true for a non-finite `ts`, so such a row is re-fetched and re-warned on every run forever and can never be retired by the watermark. (b) scripts/ledger-fold.ts:222-227 throws when the OLDEST row of a page has an unreadable `ts` — meaning one corrupt-ts row sitting at a page boundary makes every subsequent fold abort with nothing appended. Only `nextWatermark`'s NaN handling is tested (runner.test.ts:245); neither pager branch is. Practical risk is low (GovernorDO writes bare `Date.now()`), but the failure mode is a permanently wedged fold, not a degraded one. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-ec7eb382c6ef [completenessFindings] GAP (low, silent-loss risk not covered by any test or doc) — the watermark design assumes gate-side `ts` is monotonically non-decreasing. `GovernorDO.ledger` writes bare `Date.now()`, which can step backward on an NTP correction. A row written with `ts` below `lastFoldedTs` fails `isAboveWatermark` and is silently dropped from the record forever — the exact loss class the plan set out to close. memory/adapters/gate-ledger.md asserts the fold is lossless without naming this exception, and no test covers it. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-ac908bfe54e4 [completenessFindings] GAP (low, documentation drift the run created) — the plan document at docs/superpowers/plans/2026-08-20-ledger-fold-correctness.md:353 still publishes the superseded ts-only Produces contract: `pageLedger(brokerUrl, secret, sinceTs: number, ...)`, `readFoldState -> Promise<{ lastFoldedTs: number }>`, and Task 1's 'a non-numeric `before` is a 400'. The tree implements `pageLedger(..., since: FoldState, ...)`, a two-field `FoldState`, and a stricter `^\d+$` validation that also 400s on `-5`, `1.5`, and `1e3`. The redirect is well documented in commit messages and in memory/adapters/gate-ledger.md, but the plan — the document a reader would consult as the contract of record — was never amended and now contradicts the code. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-b474cf1a9f07 [completenessFindings] GAP (low, doc self-contradiction) — memory/adapters/gate-ledger.md's closing section still reads 'Each file covers exactly one UTC month; rows from other months are filtered out rather than rendered under a heading that would misdate them.' That was true of the pre-#38 single-month fetch. It now describes only `foldEntries`'s internal re-filter, while the Operations paragraph a few lines above correctly states that rows are ROUTED to their own month file, not dropped. As written the two passages read as opposite claims about the same run. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-36d1ac6fd144 [completenessFindings] OBSERVATION (non-blocking, pre-existing) — `readLedger` (broker/src/as/admin.ts:555) validates `before`/`beforeId` with a strict regex but still parses `limit` as `parseInt(...) || 50`, so `limit=abc` and `limit=0` silently become 50. Harmless (GovernorDO clamps to [1, MAX_LIMIT=200]) and out of the plan's scope, but it is an inconsistency with the hardening the redirect applied to the sibling params. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-c3467fb88c09 [completenessFindings] GLOBAL CONSTRAINTS — verified across the integrated tree. Append-only: `appendToLedgerFile` writes `existing + addition` and the non-ENOENT read error now rethrows (scripts/ledger-fold.ts:249-253), proven by a test that stubs an EACCES read and asserts the prior bytes are byte-identical afterward. Forward-only format: memory/adapters/gate-ledger.md carries three dated 2026-08-20 (#38) notes covering the `ok` column, the compound cursor + `id` field, and the compound watermark, each stating both shapes coexist. Secrets per-command: GATE_BREAKGLASS_SECRET is read only inside `main()` from process.env, with the `source .env && bun scripts/ledger-fold.ts` discipline stated in the file header and the adapter doc; no new ambient state. Fail toward duplication: `writeFoldState` runs only after every append, `nextWatermark` is monotone by construction (tested: 'never moves backward'), an old ts-only state file reads as (N, 0) and re-folds a tie rather than dropping it (tested), and a non-advancing cursor throws rather than folding a partial set (tested). No live deploys: no task in this run touched wrangler or deployment; Task 6 remains unexecuted. TDD: satisfied in substance (every behavior change carries a committed passing test), unverifiable in commit ordering. — disposition: acked:process — squashed commits; tests verified discriminating in-run
- completenessFindings-ee028f5a82d5 [completenessFindings] DELIVERABLES — no task reported failed or blocked, and every Create:/Modify: path named by the plan exists on the tree: broker/src/governor.ts, broker/src/as/admin.ts, broker/test/governor.test.ts, broker/test/routing.test.ts, scripts/lib/ledger-fold.ts, scripts/lib/ledger-fold.test.ts, scripts/ledger-fold.ts, scripts/ledger-fold.runner.test.ts, scripts/vitest.config.ts, scripts/package.json (test script = `vitest run && bun test package-manifest.test.ts`), memory/adapters/gate-ledger.md. Tasks 2 and 4 landed in the prior run and are ancestors here (60d8c0a, bd5e4ff). memory/ledger/.fold-state.json is not gitignored (`git check-ignore` rc=1), so the watermark can travel with the record as the adapter doc promises. No missing deliverables. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-bb3cbcde3db5 [completenessFindings] VERDICT — no blocking gap. HEAD is the derived tree, ancestry is complete, both suites are green, all six global constraints hold on the integrated result, and all four escalated CANNOT-VERIFY items are resolved (three affirmatively; the TDD-ordering one is unverifiable by construction and non-blocking). The findings above are quality/coverage gaps for follow-up, chiefly the untested `main()` orchestration that carries the watermark-advance constraint. — disposition: acked:process — squashed commits; tests verified discriminating in-run
- judgmentCalls-4d5bce1cc4e3 [judgmentCalls] task 3: plan-defect: the redirect's termination rule reads "terminate when the raw page is empty OR **every** row in it is at-or-below the compound watermark". I implemented "**any** row at-or-below" (the existing `crossedWatermark` signal, now compound). Rationale: the gate serves a total order of (ts DESC, id DESC), so the first at-or-below row proves every row after it is at-or-below too — the two rules reach the same fixed point, mine one page earlier, and neither can drop a row. The literal "every row" rule would make the watermark decorative: page 1 nearly always holds at least one new row, so the pager would re-walk the entire ledger from the top on every run, growing the dedupe set unboundedly and re-fetching the whole record each time. The LEDGER_LIMIT coupling the redirect actually targeted is removed either way; only the empty-page termination is new. Documented in a code comment at the `if (crossedWatermark) return out;` site and in memory/adapters/gate-ledger.md. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- judgmentCalls-9b8b8796371e [judgmentCalls] task 3: plan-defect: the redirect's Produces contract keeps `pageLedger(brokerUrl, secret, sinceTs: number, fetchImpl?)`, but fix 1 makes the since-filter compound, so a bare `sinceTs` cannot express the join. I changed the third parameter to `since: FoldState`. `pageLedger` also now returns `LedgerRowWire[]` rather than `LedgerEntryWire[]` — the previous declaration hid the `id` field the pager itself depends on and forced a cast in the tests. No caller outside scripts/ledger-fold.ts and its test consumes either symbol (verified by grep across the repo). — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- judgmentCalls-1b5427b2622f [judgmentCalls] task 3: A row with a non-finite `ts` is treated as always-above-the-watermark so a corrupt timestamp can never be mistaken for the bottom of the walk. main() warns and skips such rows at append time (and no longer counts them as folded), but nothing ever moves the watermark past them, so they would be re-fetched and re-warned on every subsequent run. This cannot occur against the real gate (GovernorDO writes `Date.now()`), and the alternative — letting one corrupt row end the walk — orphans every real row behind it. Noted rather than fixed. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- judgmentCalls-e9bcb870242e [judgmentCalls] task 3: memory/ledger/.fold-state.json does not exist yet in the repo, so the first run under this code starts at (0, 0) and re-folds the whole reachable ledger into 2026-08.md under a fresh run marker. That is the documented duplicate-over-loss posture, but it is a visible one-time effect worth expecting at the release step. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- deferredVerification-0da7a5b060a1 [deferredVerification] Task 6 Step 1 — `cd broker && bunx wrangler deploy` shipping the /ledger compound cursor to the live gate (manual) — Plan marks Task 6 as Type: release, explicitly 'On Marcus's word only (house rule: trust-core deploys are witnessed)'. The global constraint 'No live deploys inside implementation tasks' forbids this run from performing it. The code is present and structurally complete; only the witnessed deploy is outstanding. — disposition: acked:runbook — carried in the end-gate runbook for Marcus
- deferredVerification-4c97ef0276fa [deferredVerification] Task 6 Step 3 — `source .env && bun scripts/ledger-fold.ts` against the deployed gate (first paged fold: paging output, month routing, ok column in the appended run) (external) — Requires the deployed broker at BROKER_URL plus GATE_BREAKGLASS_SECRET from .env — neither reachable from the sandbox. The wire contract is independently proven on both sides (real GovernorDO + sqlite in workerd for the gate; a contract-faithful fixture gate for the runner), but no test wires the real runner against the real worker end-to-end. — disposition: acked:runbook — carried in the end-gate runbook for Marcus
- deferredVerification-ce883711e256 [deferredVerification] Task 6 Step 2 — watermark initialization decision (accept a duplicate run block in memory/ledger/2026-08.md, or pre-seat .fold-state.json from the Aug 13 fold's max ts/id) (manual) — The plan states this is to be 'Decide[d] with Marcus at the deploy'. Note the redirect widened it: the state file is now compound {lastFoldedTs, lastFoldedId}, so a pre-seat must supply both fields — a ts-only seed reads as (N, 0) and re-folds that whole millisecond. — disposition: acked:runbook — carried in the end-gate runbook for Marcus
