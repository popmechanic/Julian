# Residual manifest

<!-- derived from: .claude/ultrapowers/run-20260820-e36/report-r1.json, .claude/ultrapowers/run-20260820-e36/report-r2.json -->
<!-- disposition one of: fixed | acked[:<annotation>] | filed:<ref>[ <note>] | waived:<reason> -->

- completenessFindings-77dbc79bbccc [completenessFindings] BLOCKING — #35's counting key silently debits the stream READ budget for every socket open. `reserveLease` now computes the lease-side count for service 'stream' with `leaseCountVerb = null`, and `countSince(verb=null, sub)` runs `SELECT COUNT(*) FROM ledger WHERE sub = ? AND service = ? AND allowed = 1 AND ts >= ?` (broker/src/governor.ts:433-435, :486-487) — no verb predicate at all. But `consumeTicket` writes an **allowed** row under exactly that key on every successful socket ticket consumption: `this.ledger(now, sub, 'stream', 'ticket.consume', …, true)` (governor.ts:1053), with `sub = `lease:${leaseId}`` (governor.ts:1004) — the identical sub `reserveLease` builds (governor.ts:470). So `stream.ticket.consume` rows now count against STREAM_READ_CAP_PER_DAY. The plan's Task 2 Interfaces scope the change to 'one shared 500/day budget across recent/session/search'; `ticket.consume` is a fourth verb and is not a read. Concrete scenario: a browser session that reconnects its socket 500 times in a UTC day (each reconnect = mint + consume) has spent its entire reading allowance without issuing a single stream read — the next `stream_recent` call refuses with refusedBy 'lease', count 500. This is exactly the compensating-bound question the task-1 reviewer escalated ('socket opens remain bounded after #36 removes the ticket cap's implicit throttle'): #36 does now make mint→consume unbounded, and the bound that appeared is #35 — but it lands on the read budget rather than on socket opens, converting a connectivity problem into denial of the reading room. Invisible per-task: task 1 owns the ticket ledger row, task 2 owns the counting key, and only the join is wrong. Narrow fix: key the stream lease count on the three read verbs (e.g. `verb IN ('recent','session','search')`) instead of dropping the verb predicate. — disposition: fixed
- completenessFindings-f3952d7ccbd4 [completenessFindings] UNTESTED PATH — #37's collapse key is the access-token handle, not the ticket, so a second *different* stolen ticket on the same session raises no alarm. The dedupe query is `… WHERE sub = ? AND verb = 'ticket-reused' AND detail LIKE ?` with `%token_id=${tokenId}%` (governor.ts:1018-1021), where `tokenId` is the ACCESS token handle the ticket was minted against, shared by every ticket of that session. The implementation comment states the consequence honestly (governor.ts:1015-1017: 'a second, *different* burned ticket on the same session finds the alarm already raised and adds no row of its own'), and the plan prescribed this exact query verbatim (plan Step 3 item 2), so this is faithful implementation of the plan's own design — but the plan's global constraint reads 'the FIRST reuse of a token always writes its row', which holds only under the reading token = access token, not token = ticket. No test covers the two-different-burned-tickets-one-session case: broker/test/governor-tickets.test.ts only ever replays a single ticket (lines 241, 254-261). Mitigating and worth recording: ACCESS_TTL_SECONDS is 3600 (governor.ts:132) and token_id is a fresh `crypto.randomUUID()` per access token (governor.ts:899), so suppression is bounded to roughly one hour per session rather than permanent — which resolves the escalated 'is the suppression truly permanent' question (there is no `DELETE FROM ledger` anywhere in broker/src, so rows are permanent, but the KEY rotates hourly). — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-0efd6fc5dc73 [completenessFindings] UNVERIFIED CLAIM — the TDD global constraint ('TDD for the two behavior changes (#36, #37, #35)') cannot be verified from the integrated tree. Each task landed as a single squashed commit carrying test and implementation together (5c4bf7a, 184de2e, f47451a); no red-then-green sequence is observable. The behavior itself is proven green (609 broker + 189 sync tests pass), so this is a process claim only, but it is the third reviewer escalation of the same item and remains unconfirmable by inspection. — disposition: acked:process — squashed commits; tests verified discriminating in-run
- completenessFindings-3a71d11a9e3a [completenessFindings] PLAN GATE NOT SATISFIABLE — Task 4 requires both `tsc --noEmit` checks green; both exit 2. I ran them on the integrated tree: broker exits 2 with 1353 `error TS` lines, sync exits 2 with 238. The failure is overwhelmingly pre-existing and environmental, not a regression: 1275 of the broker errors are inside node_modules, the rest are `Cannot find module 'cloudflare:test'` (the vitest-pool-workers types are not in tsconfig), `Cannot find name '__dirname'` in vitest.config.ts / vitest.node.config.ts, and test-mcp-client. This resolves the escalated task-3 item in the affirmative on the part that matters: exactly ONE error sits in broker/src — `src/lease-auth.ts(109,3)` LeaseIdentity missing subject/flow/tokenId/sittingPin/latched — and it is pre-existing (the wave's only lease-auth.ts change is a comment, and `LeaseIdentity` at governor.ts:53 is untouched by the wave). sync/src has zero errors. So #33's compile-proof holds; the plan's Step-3/Step-4 'expected pass' was never achievable in this repo as configured, and the gate should not read a green tsc as available evidence. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-83bab58d38ad [completenessFindings] LOW / UNTESTED — the #37 dedupe LIKE pattern degenerates to a match-everything wildcard when the ticket row carries no token_id. `consumeTicket` coerces a null/undefined token_id to the empty string (governor.ts:1003), which makes the probe `detail LIKE '%token_id='` — a substring of EVERY ticket-reused detail, since all of them end in `token_id=<value>`. Any prior alarm on that lease would then suppress this one. Not reachable through the HTTP surface: the sole production caller refuses before minting when `identity.tokenId === null` (broker/src/exchange.ts:305-311), so ticket rows always carry a UUID; the path is reachable only by a direct DO call or a pre-B3 migrated ticket row inside its 60-second TTL. No test covers it. Separately I checked and can rule OUT the adjacent prefix-collision worry: token ids are fixed-length `crypto.randomUUID()` (36 chars), so no id can be a strict prefix of another. — disposition: fixed
- completenessFindings-576282ec2426 [completenessFindings] OBSERVATION — Task 3's declared Modify list was not fully realized. The plan names `shared/gate-contract.ts`, `broker/src/as/admin.ts`, and `sync/src/auth.ts`; only gate-contract.ts changed (git diff --stat over the wave). This is defensible rather than a missing deliverable — plan Step 3 says to fix consumers 'by narrowing' only where the compiler complains, and I verified both consumers already discriminate before reading fields (sync/src/auth.ts:107 `body.active ? …`, :224 `body.ok ? …`) and both producers already send every now-required field unconditionally (broker/src/as/admin.ts:116-126 activeLease, :132-136 activeLegacy, :356-361 the consume-ticket ok body). Recording it so the gate does not read the shortfall as an omission. Also resolved while checking: the escalated cross-worker deploy-skew worry about the ok-arm's now-required `token_id` is unfounded — `token_id: result.tokenId` has been sent unconditionally since the commit that introduced consume-ticket (6b44dcd), so no deployed gate build ever omitted it. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-aabdce3f3a26 [completenessFindings] VERIFIED (no gap) — recording the escalations that the integrated tree clears, so the gate need not re-ask. (a) 'Cap semantics change only for stream': the pre-existing per-verb independence test for mail passes unmodified — broker/test/governor-leases.test.ts:416-417 asserts `mail.list` opens a fresh bucket after `mail.send` reservations, and the suite is green. (b) 'Whole broker suite stays green with the new counting key' and 'whole-suite green beyond governor-tickets.test.ts': 24 files / 609 tests pass, including governor-tickets, stream-verbs, exchange, admin, and router-tickets. (c) 'Type-only means type-only': the wave touched no test file for Task 3 and no runtime value shape; ConsumeTicketError is genuinely closed — consumeTicket has exactly four refusal returns yielding only 'unknown' | 'reused' | 'expired'. (d) The #35 test is sound rather than tautological: it seeds 300 `recent` + 199 `search` allowed rows under one lease sub, proves the 500th reservation under a THIRD verb (`session`) is still allowed, then proves the 501st under `recent` refuses with count exactly 500 — arithmetic that only works if the budget is shared. (e) The pre-existing reuse assertion at governor-tickets.test.ts:241 was changed 2→1; the plan predicted it would pass untouched, which was simply wrong about that test's content — the edit is required by #37 and is a permitted behavioral change (the 'zero behavioral test edits' constraint binds Task 3 only). — disposition: acked:verified-in-run
- completenessFindings-0b1109327a73 [completenessFindings] MINOR — index residual filtering on the new null-verb query, as the task-2 reviewer flagged. `idx_ledger_sub` is `(sub, service, verb, allowed, ts)` (governor.ts:376); the null-verb predicate uses only the `(sub, service)` prefix and then residual-filters `allowed` and `ts`, so SQLite scans every row for that lease's stream service within the index range rather than seeking. Bounded in practice by the caps themselves (a lease cannot accumulate more than ~500 allowed stream rows/day plus refusals), so I judge this a non-issue at current scale — but note that if finding 1 is fixed with `verb IN (…)` the same residual-filter shape persists, and if it is fixed by keeping a verb predicate the index seeks cleanly again. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- judgmentCalls-97a0480a8911 [judgmentCalls] task 1: plan-defect: the plan's Step 4 states the pre-existing test 'a sequential second presentation is reused too, and the burn is permanent' still passes unchanged. It does not — it asserts `g.entries(10).filter(e => e.verb === 'ticket-reused')` has length 2, which is exactly the volume #37 collapses. I updated that assertion to 1 (broker/test/governor-tickets.test.ts:239) with a comment naming #37. This is a required consequence of the change, not a behavior divergence: the FIRST reuse row is still written and still asserted. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- judgmentCalls-467735962bb9 [judgmentCalls] task 1: The #37 collapse key is the plan-verbatim `token_id` — the ACCESS-token handle the ticket was minted against, not the ticket itself. So the collapse is per browser session, not per ticket: if ticket A on a session is replayed (row written) and later a different ticket B on the SAME session is stolen and replayed, B's first re-presentation writes no row. That is arguably narrower than the global constraint's 'the FIRST reuse of a token always writes its row' if 'token' means the ticket. I implemented the plan as written rather than diverging, because the precise fix would require putting a per-ticket identifier into the ledger `detail` string — a persisted record format that scripts/lib/ledger-fold.ts parses (it extracts `token_id=` into its own column), i.e. not task-local. Flagging for the reviewer/integrator to decide; the coarser key is documented in a code comment at the call site. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- judgmentCalls-b4a90fee2dda [judgmentCalls] task 1: The full-suite invocation `cd broker && bun run test` (all 24 files at once) fails on this machine with miniflare `ERR_RUNTIME_FAILURE` / `connect EADDRNOTAVAIL 127.0.0.1:<port>` — ephemeral-port exhaustion from leaked workerd processes (173 counted mid-run) under parallel agents. Verified environmental, not mine: after temporarily restoring the BASE contents of both files, the full suite failed identically. All 24 broker test files DO pass with my change when run in chunks (governor* 132, stream-verbs/lease-auth/scope-invariants/exchange/policy/admin 226, mcp 78, package/routing/registrar* 89, mail/approve*/authcode/device-flow 122), and `cd sync && bun run test` is green at 11 files / 189 tests. Whoever runs the acceptance gate may need to retry or reduce concurrency. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- deferredVerification-76b6795a40c5 [deferredVerification] Task 5 — deploy rider (gate/sync deploys) (manual) — The plan defines Task 5 as riding the next gate/sync deploys already queued by sibling plans, explicitly 'on Marcus's word'. Nothing is deployable from the sandbox and no config/secret change exists to verify; the gate should route this to human acknowledgement. — disposition: acked:runbook — carried in the end-gate runbook for Marcus
- completenessFindings-8075c3d5640b [completenessFindings] **#36 removed the only backpressure on ticket-mint RATE, and nothing replaced it (untested, unledgered, unmetered).** `TICKET_MINT_CAP` was doing two jobs: bounding live redeemable tickets (the job #36 names) and, incidentally, bounding how fast a session could mint at all — because spent rows held their slots for the full 60s TTL, a mint/consume loop was capped at 10 mints per minute per lease. After `broker/src/governor.ts:984-988` counts `used = 0` only, that second bound is gone. `broker/src/exchange.ts:315` has no `reserveLease` on the success path and deliberately writes no ledger row ("a ticket minted and never presented is not an act"), so an authenticated exchange session can now drive governor storage at request rate with no counter and no trace. It is self-limiting — `mintTicket`'s own prune deletes expired ticket rows, so steady state is roughly (mint rate x 60s) rows rather than unbounded growth — and it requires a living session token, so this is not an anonymous DoS. But the plan's Task 1 "Produces" clause claims only that the slot frees; it never re-derives what the cap was also buying, and no test bounds the new behavior. The code comment at governor.ts:179-183 acknowledges the row/slot split honestly but adds no backpressure. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-284ae11ea048 [completenessFindings] **The `reuse_alarmed` migration is never exercised against storage that predates it — and unlike the reviewers' framing, this IS testable in-sandbox.** `broker/src/governor.ts:386-389` guards `ALTER TABLE lease_tokens ADD COLUMN reuse_alarmed INTEGER NOT NULL DEFAULT 0`, and its comment asserts "the default is honest for every row that predates it: no alarm rung yet." Nothing tests that assertion. `broker/test/governor-migration.test.ts`'s `rewindToB2` (line 179-183) drops `token_id` from `lease_tokens` but never `reuse_alarmed`, so every reconstruct in the suite runs over storage that already has the column. This is a coverage asymmetry inside a file that already establishes the exact pattern for the sibling column — see its test `'a pre-B3 access token with a NULL token_id still validates after the migration'` (line 253). Adding `ALTER TABLE lease_tokens DROP COLUMN reuse_alarmed` to a rewind, with a live ticket row present, would exercise the ALTER against populated storage and prove existing rows default to 0. The escalated review note called this "outside the tree"; the rewind machinery says otherwise. (The genuinely un-runnable half — the ALTER against the deployed production DO — is filed under deferredVerification.) — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-34be58118948 [completenessFindings] **Plan Task 2's stated interface contract is NOT what shipped, and the plan should not be cited later as the spec.** The plan's "Produces" clause reads: "`countSince` accepts `verb: string | null` (null = any verb of the service)." The tree ships `verb: string | readonly string[]` with the null branch deliberately deleted (`broker/src/governor.ts:454-472`). Task 1 likewise does not ship the plan's Step-3 code: the plan's ledger `LIKE` probe was replaced by a `reuse_alarmed` column. Both redirects are documented in their commit messages and both are strictly better — I verified the plan's literal Step-3 code would have VIOLATED the global constraint "the reuse alarm never dulls", because `tokenId` on a ticket row is the ACCESS-token handle the ticket was minted against (confirmed by `consumeTicket`'s `minting` lookup at governor.ts:1090-1093, which resolves the access row by that same `token_id`), so many tickets of one browser session share it and a second stolen ticket's first replay would have found the alarm already raised. The tree is right and the plan text is wrong; recording the divergence so nobody reconciles backwards. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-9c43158c28b6 [completenessFindings] **Plan Task 4's `tsc --noEmit` gate cannot pass in this repo and never could — do not record it as green.** Task 4 requires "both `tsc --noEmit` checks" expected green. Neither is. Every project-level error is pre-existing at the wave base: `broker/src/lease-auth.ts:115` TS2322 (the legacy pseudo-lease return omits `subject`/`flow`/`tokenId`/`sittingPin`/`latched`), plus `broker/test-mcp-client/*` missing `@types/node`, `sync/test/*` missing `cloudflare:test` types, and ~40 `node_modules/@cloudflare/workers-types` DOM-lib conflicts in both packages. I confirmed the wave introduced none of them: `LeaseIdentity` is byte-identical between base `97abffe` and HEAD, and the offending return statement is unchanged (base line 108 = HEAD line 115, same text). The suites — the plan's declared acceptance mode — are the real gate and they are green. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-d4a2a5afa3f9 [completenessFindings] **The ledger adapter's own reading guide now describes the old semantics.** `memory/adapters/gate-ledger.md:31` still reads "`ticket-reused`: a socket ticket used twice." After #37 a row means "the FIRST re-presentation of this ticket", and row count no longer tracks replay volume. This is the file Julian reads the fold through, so the drift lands where interpretation happens. The fold itself is unaffected — `scripts/lib/ledger-fold.ts:83-84` partitions on the verb alone and renders theft rows verbatim, never collapsing, and it does not call `countSince` — so I found no downstream reader that depended on the any-verb count (resolving the escalated REDIRECT cannot-verify item). The adapter was not in the plan's file list, so this is follow-through rather than a missed deliverable. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-d726f4b01a56 [completenessFindings] **Minor, observability:** `leaseExport()` (`broker/src/governor.ts:1233-1235`, the break-glass dump) lists token columns explicitly and omits `reuse_alarmed` — so an operator reading a dump cannot see which tickets have already alarmed. Consistent with the pre-existing treatment of `token_id`, which the dump also omits, and no test asserts otherwise. Noted, not urgent. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- judgmentCalls-efb400b5ecd7 [judgmentCalls] task 1: process: the worktree was cut from main (07a5980), NOT from BASE (6511388) — the two share no lineage beyond 07a5980, and BASE is 3 commits ahead of it. I built on the stale parent first and produced commit a45d7c4, whose BASE..HEAD diff was a 3429-deletion reverse diff that would have reverted 56 files of sibling work at merge. I caught it from the implausible 328KB review packet, ran `git reset --hard 6511388` per the anchoring protocol, and rebuilt the change on BASE. The delivered commit 48454b7's parent is exactly BASE; a45d7c4 is orphaned and must not be merged. Worth checking whether sibling worktrees in this wave were cut from the same stale ref. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- judgmentCalls-fbb1482a15df [judgmentCalls] task 1: scope: BASE already contains a prior attempt at this same task (5c4bf7a, identical commit subject), which is what the REDIRECT exists to correct. Consequently my diff is only the REDIRECT delta (#37 re-keying), not the whole task body. #36 was already implemented correctly at BASE — `mintTicket` counts `AND used = 0`, with a passing test — so I changed no #36 logic. A reviewer diffing my commit against the task's Step-3 instructions will not find the #36 SQL edit there; it is already upstream and verified green. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- judgmentCalls-8cb6469bf9e8 [judgmentCalls] task 1: divergence (cosmetic): the REDIRECT asks to fix a TICKET_MINT_CAP doc comment that 'still implies the cap bounds retained rows'. At BASE that comment had already been rewritten by 5c4bf7a and no longer implied it. I appended the retention clause the REDIRECT specifies anyway (spent rows retained until the TTL reap; the bound is live unspent ≤ cap), so the stated intent is on the page, but the edit is an addition to an already-corrected comment rather than the correction described. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- judgmentCalls-e906c013531e [judgmentCalls] task 1: shared file: broker/src/governor.ts is listed both in my FILES and in task 2's SIBLING FILES. I confined edits to three regions (the TICKET_MINT_CAP doc block, the lease_tokens guarded migration, and consumeTicket's replay arm) to keep the merge surface small, but a textual conflict with task 2 is possible if it touches the same constructor migration block. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- deferredVerification-0a5fa658f4e5 [deferredVerification] Guarded `reuse_alarmed` ALTER against a live, already-populated production Governor DO (broker/src/governor.ts:386-389) (runtime) — The definitive proof is the column being added to deployed Durable Object storage that holds existing lease_tokens rows. The sandbox constructs fresh registers; the migration test's rewind path drops token_id only. The guard pattern is correct as written and SQLite permits ADD COLUMN NOT NULL with a non-null default on a populated table, and every INSERT into lease_tokens lists its columns explicitly (governor.ts:941, 1000, 1273, 1278) so no insert breaks — but the live ALTER itself is not exercisable here. See finding 2 for the in-suite half that IS exercisable and was not done. — disposition: acked:runbook — carried in the end-gate runbook for Marcus
- deferredVerification-1719a0fc2d49 [deferredVerification] Task 5 deploy rider — broker + sync worker deploys (manual) — The plan defers it explicitly: 'Rides the next gate/sync deploys already queued by the sibling plans, on Marcus's word.' No secrets and no config change, but the deploy is gated on a human decision and on sibling plans landing first. — disposition: acked:runbook — carried in the end-gate runbook for Marcus
