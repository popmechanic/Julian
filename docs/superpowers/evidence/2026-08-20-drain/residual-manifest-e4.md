# Residual manifest

<!-- derived from: .claude/ultrapowers/run-20260820-e4/report-r1.json, .claude/ultrapowers/run-20260820-e4/report-r2.json, .claude/ultrapowers/run-20260820-e4/report-r3.json -->
<!-- disposition one of: fixed | acked[:<annotation>] | filed:<ref>[ <note>] | waived:<reason> -->

- completenessFindings-eda3816da964 [completenessFindings] NON-BLOCKING (latent race, untested): the global constraint's "stop() ... while startup is still in flight" clause is neither tested nor actually satisfied in substance. app/src/lib/connection.ts assigns the module-level `current = handle` only AFTER every await (startPersistence, connectEvents, startSync). A `stopConnection()` that lands between the first await and that assignment sees `current === null`, resolves as a no-op, and the in-flight start then installs itself — producing exactly the orphaned reader/socket/persister the module exists to prevent, and defeating the leading `await stopConnection()` "never two live connections" guard. It does not throw, so the constraint's literal wording holds. Not reachable through the shipped UI today: the App.svelte effect keys only on `ready`, which transitions false->true once (App.svelte:88,110), and the new logout hard-reloads instead of setting `ready = false`. Reachable on unmount/HMR. Fix would be to install a placeholder/promise into `current` before the first await. — disposition: fixed
- completenessFindings-a0805fcb809b [completenessFindings] NON-BLOCKING (error path): in the App.svelte LOGOUT handler (App.svelte:151-162) `clearLocalRecord()` was defensively wrapped in try/catch but `signOut()` was not. If `signOut()` rejected, `window.location.replace('/')` never runs and the user is left on a signed-in-looking page whose connection has already been torn down (socket closed, persister destroyed, OPFS record deleted) with nothing to restart it — a strictly worse end state than the pre-change `await signOut(); ready = false;`. Likelihood is low: signOut() is `um?.removeUser()` (auth.ts:74), a localStorage write. — disposition: fixed
- completenessFindings-ba5444c1256d [completenessFindings] NON-BLOCKING (browser-only residual risk to the 'logout clears' guarantee): `persister.destroy()` (connection.ts:48) is synchronous and does not await an in-flight OPFS save, and the persister holds a FileSystemFileHandle captured at start (store.ts:68, `getFileHandle('julian-chat.json', { create: true })`). A save already in flight when logout runs could commit after `clearLocalRecord()` has removed the entry, recreating julian-chat.json with the pre-logout record — which the next visitor to that browser profile would load. Behavior is implementation-dependent (a writable opened before removal). Ordering in the handler is otherwise correct (stop, clear, sign out, reload). Plan Task 6 Step 3 already asks Marcus to confirm the OPFS file is gone after logout, so the runbook covers it. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-eca2067933b4 [completenessFindings] NON-BLOCKING (new background failure mode, unverified against the live issuer): auth.ts now sets `automaticSilentRenew: true` (auth.ts:32) but no `silent_redirect_uri` is configured. If the Pocket ID client does not actually grant a refresh token for the newly-requested `offline_access` scope, oidc-client-ts's SilentRenewService will invoke signinSilent() on a timer and fail repeatedly (no silent redirect uri), raised as a silentRenewError event with no listener registered — swallowed, but recurring. Previously signinSilent() was only called from getToken() and only when `user.refresh_token` existed (auth.ts:63-65), which is unchanged, so this is not a functional regression; the signed-out path still carries the failure honestly, as the plan's Step 5 anticipates. Plan Task 6 Step 1 is the gate for this. — disposition: fixed
- completenessFindings-077701ac93f3 [completenessFindings] NON-BLOCKING (unverifiable by construction, but substantively satisfied): TDD red-before-green ordering cannot be observed — each task landed as a single squashed commit carrying test + implementation together, exactly as the plan's Commit steps instruct. I verified the substance the rule protects instead: every new test is discriminating against BASE behavior. exchange.test.ts:178-189 would read 3 without the `if (!this.isRevoked)` guard; events.test.ts:61-69 would leave `aborted === false` because BASE connectEvents ignores a second argument; store.test.ts's stale test never sees 'stale' without `consecutiveThrows` (terminalCount() is stubbed to 0); store.test.ts's reset test is explicitly built to span FOUR throws around a non-terminal resolution so deleting `consecutiveThrows = 0` turns it red; store.test.ts's handle test would throw on `handle.sync.destroy` against BASE's bare-Synchronizer return; connection.test.ts could not even import a nonexistent module. — disposition: acked:process — squashed commits; tests verified discriminating in-run
- completenessFindings-906e7d0b43ec [completenessFindings] NON-BLOCKING (unremarked behavior change): `await startPersistence()` was removed from the boot effect (App.svelte:70-73) and now runs inside startConnection, i.e. only once SetupScreen calls onReady. A user parked on SetupScreen (health reporting needsSetup) no longer has the OPFS record loaded or auto-saving, where previously it loaded at boot. Harmless — chat is not rendered before `ready` and nothing is lost — but the plan does not call it out. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-a0fdb967638d [completenessFindings] VERIFIED — global constraint 'never wipe the in-memory MergeableStore': clean. `grep -rn 'delTables|delValues'` over app/src and server returns nothing; logout is stopConnection -> clearLocalRecord (file deletion only) -> signOut -> window.location.replace('/'). — disposition: acked:verified-in-run
- completenessFindings-2dafeb74f09f [completenessFindings] VERIFIED — global constraint 'providers stay TOTAL': the #43 throw counter lives entirely inside the existing try/catch in createTicketUrlProvider (store.ts:145-148); no new exit path, no reject, no throw. `provideUrl` still returns `Promise<string>` from a `for(;;)`. — disposition: acked:verified-in-run
- completenessFindings-02e16111e5c4 [completenessFindings] VERIFIED — global constraint 'stop() is idempotent': connection.ts:39-41 latches `stopped`; connection.test.ts covers double-stop (sync.destroy called exactly once), stopConnection with no live connection, a superseded handle stopping nothing, local mode (startSync -> null), no-OPFS (startPersistence -> null), and a failing sync leg releasing what was already acquired. — disposition: acked:verified-in-run
- completenessFindings-c2e6dc0c4663 [completenessFindings] VERIFIED — global constraint 'local mode keeps working': the App-level path I could not judge from any single task's diff holds on the integrated tree. SetupScreen.svelte:12 uses isSignedIn(), which returns true unconditionally when authEnabled() is false (auth.ts:47), so onReady() fires and startConnection runs unauthenticated; startSync returns null with absent env vars (store.ts:166-169, covered by store.test.ts 'returns null when sync env vars are absent'); connectEvents still connects with a null token (events.ts:65-67). Only real-browser boot remains unexecuted. — disposition: acked:verified-in-run
- completenessFindings-4a1569ef27ca [completenessFindings] VERIFIED — typecheck: `bun run check` (svelte-check, 262 files) reports exactly 2 errors, both the known unrelated issue #44 (`Cannot find module 'node:fs'/'node:url'` in src/lib/api.test.ts). Nothing from this run. That settles two escalated CANNOT-VERIFY items: the `fetchImpl: typeof fetch = (...args) => fetch(...args)` default is assignable, and `events` narrows to non-null inside the stop() closure in connection.ts:41. — disposition: acked:verified-in-run
- completenessFindings-14947d860536 [completenessFindings] VERIFIED — no second owner: `grep -rn 'startPersistence|connectEvents|startSync' app/src` outside tests shows connection.ts as the only app-path caller. debug-auth.ts:55 discards startSync's return, so widening it to SyncHandle | null breaks no caller. — disposition: acked:verified-in-run
- completenessFindings-fb858a27b9e6 [completenessFindings] VERIFIED — Task 5 gate: `cd app && bun run test` is green — 13 files, 100 tests, 0 failures. — disposition: acked:verified-in-run
- judgmentCalls-4f4cbc9d4ef4 [judgmentCalls] task 3: plan-defect: rewrote the two new createTicketUrlProvider test assertions (stale-on-3-throws, reset-on-success) from checking the ambient syncPhase() global to observing phase transitions via onSyncPhase during the scenario — the plan's literal code was test-order-dependent because `phase` is shared module state never reset between tests (demonstrated by the pre-existing terminalCount-based stale test leaving phase='stale' with nothing to clear it). Fix is confined to store.test.ts, no cross-task interface touched. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- judgmentCalls-26cd3a027a08 [judgmentCalls] task 3: Deviated from the plan's stated fallback for the startSync handle test ("assert the handle shape by type-only compilation") because vitest's esbuild transform doesn't typecheck, so that fallback would never fail red first. Instead added vi.mock() for 'reconnecting-websocket' and 'tinybase/synchronizers/synchronizer-ws-client' (scoped to store.test.ts, confirmed no other test in the file constructs either) to get real, order-independent, fail-first-then-pass coverage of the new SyncHandle shape. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- judgmentCalls-b094086bff2b [judgmentCalls] task 4: plan-defect: the plan's startConnection body acquires the persister, then the event reader, then awaits startSync with no guard. If the sync leg rejects, the caller's `void startConnection(...)` swallows it and the already-acquired reader and persister keep running with no handle to stop them — `current` is never assigned, so they are permanently unreachable. That orphan is precisely what this module exists to prevent ("what startConnection acquires, stop() releases — and nothing else holds a reference"). Diverged by wrapping the connectEvents + startSync legs in a try/catch that releases what was acquired and rethrows. Task-local (connection.ts only), no change to the Produces interface, and pinned by the test 'a failing sync leg releases what was already acquired'. — disposition: fixed
- judgmentCalls-da57db56a060 [judgmentCalls] task 4: plan-defect: the plan's LOGOUT handler awaits clearLocalRecord() unguarded between stopConnection() and signOut(). clearLocalRecord rethrows non-NotFoundError failures by design (a silently-uncleared record outliving its logout is worse than a loud one), so a permission or lock error on the OPFS file would abort the handler before signOut() ever ran — leaving the OIDC user, and its refresh token, in localStorage while the user believes they logged out. Diverged by wrapping only that call in try/catch, logging the failure and completing signOut() + reload regardless. Task-local (App.svelte only). Tradeoff accepted deliberately: a stale local cache survives into the next login, but the page reloads unauthenticated so it never syncs, whereas a trapped signed-in session is a security failure. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- judgmentCalls-08eceb43fa6e [judgmentCalls] task 4: scope: app/src/lib/auth.test.ts is modified but is not named in this task's declared FILES. The task's Step 5 changes auth.ts behavior (scope and automaticSilentRenew) and explicitly defers verification to the manual task, but the TDD global constraint binds every behavior change to a failing test first. Added a single test-only assertion on the UserManager constructor config, run RED before the edit. Test-only and additive — it cannot alter production behavior — but flagging it as an out-of-FILES path for the reviewer's judgment. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- judgmentCalls-9ae6d2eaaed6 [judgmentCalls] task 4: unverified-by-suite: the live half of the offline_access change cannot be tested here. The suite asserts the requested configuration, not that the Pocket ID issuer actually grants a refresh token. If the issuer declines, signinSilent() fails exactly as it does today and the UI's signed-out path carries it; the plan assigns that verification to the manual task. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- deferredVerification-fdf019636b6b [deferredVerification] auth.ts — offline_access scope + automaticSilentRenew background token renewal (plan Task 6 Steps 1-2) (external) — Requires the live Pocket ID issuer at souls.exe.xyz plus Marcus's admin access to confirm the client actually grants refresh tokens for offline_access. The unit test (auth.test.ts:73-86) can only prove the two UserManager settings are requested. If the issuer declines refresh tokens, automaticSilentRenew will fail on a timer with no silent_redirect_uri configured (see findings). — disposition: acked:runbook — carried in the end-gate runbook for Marcus
- deferredVerification-aa9a17bf2217 [deferredVerification] Logout smoke — socket closes, OPFS julian-chat.json is gone, page reloads signed out, and no further /socket-ticket mints appear in the gate ledger after the logout timestamp (plan Task 6 Step 3, the #4c proof) (browser) — Needs a real browser with devtools and the gate ledger. The unit suite proves stop() calls ws.close()/client.reset()/persister.destroy() and that clearLocalRecord targets 'julian-chat.json', but not that the live socket actually closes or that ticket minting truly ceases. — disposition: acked:runbook — carried in the end-gate runbook for Marcus
- deferredVerification-c1c0fc6c10c4 [deferredVerification] clearLocalRecord() vs an in-flight OPFS save from the destroyed persister (browser) — persister.destroy() does not await a pending write, and the persister holds a FileSystemFileHandle captured before removal. Whether a writable opened pre-removal can recommit the file is implementation-dependent and only observable in a real OPFS implementation. — disposition: acked:runbook — carried in the end-gate runbook for Marcus
- deferredVerification-87e76fa8eaa0 [deferredVerification] Local mode end-to-end — no VITE_OIDC_ISSUER: unauthenticated boot, sync offline, events still connect (browser) — Every leg is unit-covered on the integrated tree (isSignedIn() true, startSync null, connectEvents with a null token), but the composed boot through SetupScreen -> onReady -> startConnection has never been executed in a browser. — disposition: acked:runbook — carried in the end-gate runbook for Marcus
- completenessFindings-5a188b092b7d [completenessFindings] BLOCKING GAP (confirmed, code-read + library-source verified) — in the exact #43 scenario this plan exists to fix, startSync() never settles, so the connection module's central invariant does not hold. Chain: when client.ticket() throws on every iteration (the literal shipped-bundle defect), createTicketUrlProvider's returned provideUrl loops forever and never resolves (store.ts:151-176, correct and TOTAL by design). ReconnectingWebSocket._connect() is therefore parked awaiting _getNextUrl (node_modules/reconnecting-websocket/dist/reconnecting-websocket-cjs.js:511-524) and never constructs an inner WebSocket, so neither 'open' nor 'error' is ever dispatched, and _connectTimeout is set only AFTER the URL resolves (same file, line 523) so no timeout rescues it. createWsSynchronizer resolves ONLY on one of those two events (node_modules/tinybase/synchronizers/synchronizer-ws-client/index.js:849-864 — note it never rejects, it only resolves or hangs). Hence store.ts:204 `await createWsSynchronizer(...)` hangs forever, so connection.ts:71 `sync = await startSync(getJwt)` hangs, so connection.ts:90 `current = handle` is never reached. Consequences: (a) stopConnection() (connection.ts:95-98) becomes a SILENT NO-OP — `current` is null, so `current?.stop()` does nothing, and the generation bump cannot cancel a start that will never reach another await boundary; (b) the events reader (connection.ts:70) and the OPFS persister (connection.ts:65) are orphaned with no owner, plus the ticket-minting RWS; (c) App.svelte:100's `.catch(connectionFailed('start'))` never fires because nothing rejects. This directly contradicts connection.ts's own stated contracts at lines 13-19 ('a later start or any stopConnection bumps it, and a start that finds its own generation stale releases what it took'), lines 61-63 ('the exact orphan this module exists to make impossible'), and stopConnection's docstring 'supersede any start in flight'. Blast radius is bounded on the LOGOUT path only, by App.svelte:181 window.location.replace('/') killing the page; it is NOT bounded on the $effect-cleanup/remount path (App.svelte:102), where a remount leaves the first events reader + persister + socket running forever while a second set is installed. Mitigation that does work: the new consecutive-throw counter still flips the pill to 'stale' after 3 throws, so the user-visible half of #43 IS fixed — this finding is about resource ownership, not about the pill. — disposition: fixed
- completenessFindings-3d253f40781c [completenessFindings] UNTESTED PATH (structural) — startSync owns a self-connecting resource it never guarantees to release on its own failure, and its caller structurally cannot. store.ts:196 constructs `new ReconnectingWebSocket(provideUrl, ...)`, which begins connecting immediately (constructor calls _connect(), dist line 205; startClosed defaults false at line 121). store.ts:196-214 then performs two awaits (createWsSynchronizer at 204, sync.startSync() at 213) with NO try/catch. If either rejects, the RWS is never closed, and connection.ts's release() cannot close it because it guards on `if (sync)` (connection.ts:51) and `sync` is still null — the reference only ever reaches the caller inside the SyncHandle that the throw prevented from being returned. Present reachability is low (TinyBase swallows persister errors via its internal tryCatch → save() at synchronizer-ws-client/index.js:499-508 wraps setPersisted in tryCatch(..., onIgnoredError)), so this is version-dependent luck rather than a guarantee. No test covers it: connection.test.ts:104 ('a failing sync leg releases what was already acquired') mocks startSync as *rejecting*, which asserts only that the caller releases the persister and events — it says nothing about resources startSync created internally, and rejecting is not the path the real library takes. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-e32a4737b57a [completenessFindings] UNVERIFIED CLAIM (process, not reconstructible) — TDD red-first evidence for task 3's totality test and for task 4's steps cannot be established from this tree. The packet carries squashed commits (b9b6835 carries both the totality test and the store.ts:156-166 inner try/catch guard together; 7a3b0c5 likewise), so the failing-first run cannot be replayed from here. Analytically the guard IS discriminating and I verified it by reading: without the inner try/catch at store.ts:156-166, a throwing onSyncPhase subscriber invoked synchronously by setPhase('stale') would propagate out of the catch arm, reject provideUrl, and hold RWS's _connectLock — and store.test.ts:298-333 asserts exactly that it does not. The test passes on this tree. Reported as an unverifiable process claim only; no defect implied. — disposition: acked:process — squashed commits; tests verified discriminating in-run
- completenessFindings-60dda9cc6932 [completenessFindings] VERIFIED CLEAN (checklist items resolved on the integrated tree, no action needed) — (1) stop() idempotency holds: connection.ts:83-88 latches `stopped`, stopConnection() is null-safe, and connection.test.ts covers twice-called, no-live-connection, and stop-during-startup cases. (2) Interface consumers are all updated for Promise<SyncHandle|null>: the only two non-test callers of startSync are connection.ts:71 and debug-auth.ts:55, and debug-auth discards the return value, so both are compatible. (3) Local mode holds end-to-end: auth.ts:25 `if (!cfg) return` guards the whole UserManager block, so the new addSilentRenewError registration at auth.ts:47 can never run without VITE_OIDC_ISSUER; isSignedIn() returns true when !authEnabled(), SetupScreen.svelte:25 then calls onReady(), so the connection starts, connectEvents runs, and startSync returns null → phase 'offline' (store.ts:186-189, covered by store.test.ts:385). (4) The tombstone constraint holds: no delTables()/delValues() anywhere in the logout path — the only delRow in src/ is jobs.ts:22 (ordinary jobInterest cleanup, pre-existing) and api.test.ts:11 (a test). Logout is App.svelte:165-181: stop → clear → signOut, each leg individually try/caught so a failure cannot skip later legs, always terminating in window.location.replace('/'). (5) The 'stale' phase renders distinguishably: SyncStatus.svelte:16 labels it 'reload for the new Julian' (unique text) with a gray dot (line 41) versus connecting's pulsing yellow (line 37). (6) bun run check is red ONLY for the parked unrelated cause (issue #44 — 'Cannot find module node:fs / node:url' in api.test.ts, 2 errors across 262 files), confirming the startSync signature change and the new connection.ts typecheck clean; per the plan this is knowingly not the gate. — disposition: acked:verified-in-run
- judgmentCalls-2b98692cd9b1 [judgmentCalls] task 4: Out-of-scope file modified: app/src/lib/auth.test.ts is not in my declared FILES. The redirect's item (b) directs registering um.events.addSilentRenewError(...) in auth.ts, but the existing test double defined um.events with only addUserLoaded/addUserUnloaded, so initAuth() would throw 'addSilentRenewError is not a function' and break 6 passing auth tests. I added the method to the double plus one test asserting the listener is registered and warns. The in-scope alternative (an optional call, um.events.addSilentRenewError?.(...)) would have weakened production code to accommodate a stale double for a method oidc-client-ts always provides; I judged fixing the double the honest move. No other behavior in that file changed. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- judgmentCalls-ca934902dcda [judgmentCalls] task 4: App.svelte changes (redirect items 2 and 3) are not covered by automated tests: this repo has no App.svelte harness — App.svelte was untested at BASE, and the component tests present (ChatView, FaceHeader, ScreenEmbed) exercise extracted component modules, not the shell. Both edits are small and type-checked by svelte-check, but the unhandled-rejection catch and the always-completing logout are verifiable only by the plan's Task 6 manual smoke. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- judgmentCalls-d45ad6c13ef9 [judgmentCalls] task 4: Recorded, not fixed (as the redirect directs): the persister-destroy vs in-flight-OPFS-save race remains open. It is named in a comment above release() in connection.ts — TinyBase's Persister exposes no drain to await, so a browser may land a save started before destroy() after logout deletes the file, resurrecting the local record. Per the redirect I did not edit the plan's Task 6 checklist. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- judgmentCalls-9484bd0eb10d [judgmentCalls] task 4: stopConnection() now increments the module-level generation on every call, including when no connection is live. This is intentional (a stop must supersede any start already in flight, which may not have installed itself yet) and the existing 'stopConnection with no live connection is a no-op' test still passes, but it does mean the counter advances on idle stops. Harmless — only relative equality against a start's captured `mine` is ever compared. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- judgmentCalls-d6dee7c57d20 [judgmentCalls] task 4: startConnection's `await current?.stop()` can reject if a previous handle's teardown throws, in which case startConnection rethrows before acquiring anything. That is loud rather than silent and is now caught at the App.svelte call site (flipping the pill to 'stale'), but it means one bad teardown can block the next connect until a reload. I left this as-is rather than swallowing it, since a teardown that failed leaves resource state genuinely indeterminate. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- deferredVerification-b423ca4bc117 [deferredVerification] Task 6 Step 1 — Pocket ID client issues refresh tokens for the offline_access scope (external) — auth.ts:30 now requests 'openid profile offline_access' and auth.ts:32 sets automaticSilentRenew, but whether the issuer actually grants refresh tokens is a server-side client setting on the self-hosted Pocket ID instance, which the sandbox cannot reach or administer. — disposition: acked:runbook — carried in the end-gate runbook for Marcus
- deferredVerification-4e21c1a94047 [deferredVerification] Task 6 Step 2 — session renews without a full-page redirect past access-token life (browser) — Requires a live signed-in browser session held past token expiry against the real issuer. Note a latent risk worth watching during this smoke: automaticSilentRenew is enabled but no silent_redirect_uri is configured, so background renewal may fail every cycle — auth.ts:47-49 now logs that via addSilentRenewError, and getToken()'s explicit signinSilent still covers the user-facing path, but the console warning is the only signal. — disposition: acked:runbook — carried in the end-gate runbook for Marcus
- deferredVerification-081a7200d641 [deferredVerification] Task 6 Step 3 — logout smoke: socket closes, OPFS file deleted, no further /socket-ticket mints after the logout timestamp (browser) — Needs devtools to watch the socket, navigator.storage.getDirectory() in a real console to confirm the OPFS file is gone, and the gate ledger to confirm minting stopped. This is also the check most likely to expose finding 1: if the connection was mid-hang when logout fired, stopConnection() no-ops and only the page reload ends the socket — so confirm the ledger explicitly rather than inferring from the reload. — disposition: acked:runbook — carried in the end-gate runbook for Marcus
- deferredVerification-e028dc4242e6 [deferredVerification] Sync pill rendering the 'stale' terminal state to a watching user (browser) — Statically verified as distinguishable (SyncStatus.svelte:16 unique label, line 41 gray dot vs line 37 pulsing yellow), but the rendered pill was not observed; only a browser can confirm it is legible and visibly terminal in situ. — disposition: acked:runbook — carried in the end-gate runbook for Marcus
- deferredVerification-ec0b14b41ccd [deferredVerification] Known OPFS write/delete race recorded in connection.ts:44-48 (browser) — The module documents, rather than fixes, that Persister.destroy() does not drain an already-started OPFS save, so a write can land after logout deletes the file and resurrect the local record. TinyBase exposes no drain to await, so this is unfixable at this layer; it can only be observed in a real browser under a logout raced against a save. — disposition: acked:runbook — carried in the end-gate runbook for Marcus
- completenessFindings-9b47e5a7c5cf [completenessFindings] GAP (most serious, untested): startSync can still hang forever, and the orphan it was meant to close survives in a narrower form. The stale watch (store.ts:262-278) converts only phase 'stale' into a rejection, and 'stale' is reached only by 3 consecutive provider THROWS or client.terminalCount() >= 3. Verified in exchange.ts: signedOutState() (line 168-171) and retryState() (line 173-178) BOTH set this.terminal = 0. So a sustained signed-out state or an ordinary server outage (503/429 -> retryState) loops in createTicketUrlProvider forever, never resolves a URL, never opens the socket, and createWsSynchronizer never settles. startConnection then parks at connection.ts:71 `sync = await startSync(getJwt)` holding a LIVE events reader (line 70) and persister (line 65) that were already acquired, with no handle installed; a subsequent stopConnection() bumps the generation and returns immediately because `current` is null, so it cannot release them. That is exactly the 'live reader and persister that no handle can ever stop' the module's own comment (connection.ts:13-19) says must be impossible. Blast radius is bounded in practice because the logout path ends in window.location.replace('/'), but a component remount during an outage leaks. No test covers it (store.test.ts:415 covers only the throwing-ticket variant). — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-ce396d9c700c [completenessFindings] GAP (untested interleaving): the socket close listener can overwrite 'stale' with 'offline'. store.ts:282-287 guards only `syncPhase() !== 'revoked'`. On the createWsSynchronizer-rejects path the underlying socket may already exist, so `ws.close()` in startSync's catch (line 318) produces a real close event whose listener sets 'offline' asynchronously — potentially landing AFTER App.svelte's connectionFailed sets 'stale' (App.svelte:95-97), turning a terminal, reload-me state back into a retryable-looking one. Not reachable on the #43 path — I verified against node_modules/reconnecting-websocket/dist/reconnecting-websocket-cjs.js:363-371 that close() returns early with no `_ws` and fires no event — but the mocked RWS in store.test.ts has an inert addEventListener, so no test can see this interleaving either way. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-076b1b177104 [completenessFindings] GAP (untested, adjacent to a recorded race): stopConnection() does not await an in-flight start. During logout (App.svelte:174-189) `await stopConnection()` resolves while a superseded start may still be inside `startPersistence()`; that call does `persister.load()` + `startAutoSave()` (store.ts:81-82), which can rewrite julian-chat.json AFTER `clearLocalRecord()` deleted it, resurrecting the record the logout meant to end. connection.ts:44-48 records the adjacent 'destroy() does not drain an in-flight OPFS save' race but not this one, and nothing tests it. Task 6 Step 3's manual OPFS check is the only thing that would catch it. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-dbc03fb9d9c3 [completenessFindings] DOC DRIFT (not a defect): the plan's Task 3 Interfaces block still declares `startSync(getJwt, client?): Promise<SyncHandle | null>` with no rejection path, while the integrated code adds SyncStaleError rejection (store.ts:227-238, 293-327). The one in-repo caller does handle it correctly — connection.ts:71-82 catches, calls release(), returns INERT when superseded and rethrows otherwise — so this is stale documentation, not an unhandled contract change. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-d87e78aafc7a [completenessFindings] CLOSED from the escalated cannot-verify list — suite: `cd app && bun run test` is green on the integrated tree: 13 files, 107 tests passed, 0 failed. Includes exchange (17), events (9), store (18), connection (13), auth (7) and the component-module tests. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-6c7cd205dc0f [completenessFindings] CLOSED from the escalated cannot-verify list — App.svelte type-check: `bun run check` (svelte-check) reports exactly 2 errors, both the known pre-existing `Cannot find module 'node:fs'/'node:url'` in src/lib/api.test.ts (parked issue #44, which the plan explicitly excludes from its gate). Zero errors in App.svelte, connection.ts, or store.ts — the App.svelte cleanup arrow and the connection rewiring type-check clean. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-deb326abee7f [completenessFindings] CLOSED from the escalated cannot-verify list — superseded-start behavior with a real setPhase: connection.test.ts now threads the REAL setPhase/syncPhase through the store mock (test at line 217), drives the exact `.catch(() => setPhase('stale'))` call App.svelte makes, and asserts the phase stays 'synced'. The concern that the assertion depended on a stubbed setPhase no longer applies. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- completenessFindings-66aad6979f67 [completenessFindings] VERIFIED the load-bearing library claim behind the cancel design: store.ts:158-168 asserts that RWS close() sets `_closeCalled`/`_shouldReconnect = false` before its `if (!this._ws) return`, and that the `_getNextUrl` continuation checks `_closeCalled` before constructing a WebSocket. Both confirmed against the installed dist (close: lines 363-371; _connect continuation: lines 511-516). So the cancel path's ticket-less URL `${base}/${STORE_PATH}` can never open an unauthenticated socket — it is discarded. (Residual, benign: `_connectLock` stays true forever on that path, which is harmless since `_shouldReconnect` is false.) — disposition: acked:verified-in-run
- completenessFindings-32d325b29cd0 [completenessFindings] GLOBAL CONSTRAINTS all hold on the integrated tree: (1) no store.delTables()/delValues() anywhere under app/src — grep is empty; logout is file-delete + location.replace('/'). (2) Provider stays TOTAL — the cancel exit RESOLVES, and setPhase in the catch arm is itself try/caught against a throwing subscriber (store.ts:196-205), with a dedicated test at store.test.ts:306. (3) stop() idempotent — connection.test.ts:85 and the module's `stopped` latch. (4) Local mode — startSync returns null with no env vars (store.test.ts:480) and connection.test.ts:111 proves a null sync leg still stops cleanly. (5) TDD ordering: unverifiable from the packet (both this run's commits, cd33aed and 333dceb, carry test+implementation together), but the tests are mutation-discriminating by construction and say so — e.g. store.test.ts:454-459 names `provideUrl.cancel()` as the deletion that turns it red, and store.test.ts:346-355 documents why a two-throw scenario would NOT discriminate. — disposition: acked:process — squashed commits; tests verified discriminating in-run
- judgmentCalls-fe0596f0f455 [judgmentCalls] task 3: Residual (documented in code, unavoidable): on the stale-rejection path the losing createWsSynchronizer promise stays pending forever — the library exposes no abort. It holds only a socket that has been closed and can never reconnect (_shouldReconnect === false), so it is an inert retained object, not a live leg. Called out in a comment in startSync's catch arm. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- judgmentCalls-4d6939b6d971 [judgmentCalls] task 3: Test-fidelity gap: the reconnecting-websocket mock in store.test.ts now records each construction and the url provider it was handed, but deliberately does NOT call that provider itself (the real library calls it from _connect()). Tests that need the provider loop to run drive it explicitly. This keeps every other startSync test from paying for a background retry loop it did not ask for, but it does mean the mock is not a behavioral stand-in for _connect() — the close/_closeCalled interaction is verified by reading the dist source, not by executing it. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- judgmentCalls-c148a55201c3 [judgmentCalls] task 3: Pre-existing and unrelated: `bun run check` (svelte-check) reports 2 errors, both in app/src/lib/api.test.ts ("Cannot find module 'node:fs'/'node:url'" — missing @types/node). Reproduced identically at BASE in the main checkout, so it is not introduced by this task; left untouched as it is outside my declared file scope. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- judgmentCalls-881247552e0d [judgmentCalls] task 3: Minor task-local cleanup not named in the task text: deleted the module-private `sleep()` helper in store.ts, which became unreachable once all three call sites moved to the new cancellable `sleepInterruptibly`. It was never exported, so no caller or sibling task can consume it; leaving it would be dead code. — disposition: acked:end-gate — open residual, carried in the drain end-gate report
- deferredVerification-ff8ef9edf53c [deferredVerification] app/src/lib/auth.ts — offline_access scope + automaticSilentRenew (Task 4 Step 5, Task 6 Steps 1-2) (external) — Whether the Pocket ID client actually issues refresh tokens for the offline_access scope, and whether the session renews without a full-page redirect past access-token life, can only be established against the live issuer with admin access. auth.test.ts covers the settings shape, not the issuer's response. — disposition: acked:runbook — carried in the end-gate runbook for Marcus
- deferredVerification-e47a725f0cde [deferredVerification] app/src/App.svelte — the ready $effect (startConnection/stopConnection wiring), the connectionFailed pill, and the LOGOUT handler (browser) — The plan itself records App.svelte as untestable in this harness (no component mount infra). The edits type-check clean and the logic is mirrored by connection.test.ts, but no test executes the Svelte effect lifecycle or the button handler. — disposition: acked:runbook — carried in the end-gate runbook for Marcus
- deferredVerification-848add5f8b6d [deferredVerification] Logout proof: socket closed, OPFS julian-chat.json gone, and no further /socket-ticket mints in the gate ledger after the logout timestamp (Task 6 Step 3, the #4c proof) (browser) — Requires devtools, navigator.storage.getDirectory() in a live console, and the gate's ledger. This is also the only check that would surface the in-flight-start OPFS resurrection race listed in the findings. — disposition: acked:runbook — carried in the end-gate runbook for Marcus
