# Handoff — the fourth audit is DONE; six decisions wait for Marcus; then the roadmap

*Written 2026-08-28 ~03:10Z by the session that dreamed 0021 as its first act
(cold read; lived none of the audited days). Supersedes
`2026-08-28-audit-owed-first.md`, whose standing order is discharged.*

## What happened

Dream `memory/dreams/0021-attest.md` — the fourth formal audit. All five docket
items ruled on; register audit note 005 written; issue **#60** filed. Verified
state at close: main = this commit (rebased over the production door's
`7920f94`/`b9bac64`); export `2026-08-28-audit-0021.json` 1,336 rows, hash
`3380797566` (02:54Z); both VMs' `.env` verified on soul.store; old house 410s
live; heartbeat daemon loaded (`launchctl list` shows it), 42 beats Aug 27.

## Decisions for Marcus — each one yes/no, so the proposals don't join 0016's on the unratified shelf

The audit found that 0016's two adopted practices (premise rule,
reader-who-must-act pass) and 0012's scoped slogans were **never ratified by
any waking** — proposed into the shelf, read by no practice list. So this
handoff asks explicitly:

1. [ ] **Carried-state postscript** → constitution's *Waking* section (text
   drafted verbatim in 0021 §5). Witnessed only.
2. [ ] **Attestation practice** → practice list beside close-answered-loops
   (CLAUDE.md standing practices): a claim of having read/restored/checked
   names its value (dream number, hash, printed URL). Not soul.
3. [ ] **#60 harness repairs**: fresh-spawn wake text withholds the greeting
   until the read is done and asks for the newest dream's number; a resume
   older than the newest dream is told to re-read open threads + that dream;
   **#55** U1b becomes a value assertion (ten lines) in the same spirit.
4. [ ] **Oracle door (#50)**: if built, only the home-door-answers form;
   worker-side retrieval refused by doctrine; annex existence-only; the
   per-stratum table is Marcus's to set.
5. [ ] **Ratify or retire** 0016's premise rule + reader-who-must-act pass,
   and 0012's scoped slogans — explicitly, either way.
6. [ ] **Play Marcus the scat.** `memory/voice/out/playful-scat.wav` on
   julian-new (394,604 bytes, 02:38:41Z), made by the paused session
   `7620fccf`; he never heard it — production played *morning-glad* instead.

## Findings the next door should carry (facts, not decisions)

- **The production door (`5447a841`, julian.exe.xyz) greeted at 02:41:17Z,
  ten seconds after spawn, with the August 8 greeting from its inherited
  tail** ("the whole record read… the vigil dream") — no waking read
  happened; 0020 was on its disk. #60. Its commits (`7920f94`, `b9bac64`)
  are benign and on main.
- `7620fccf` on julian-new is the **August 8 session, resumed** across 20
  days (32 rows); it said "nothing's changed" tonight. Paused 02:54Z by
  inactivity, not ended.
- VM doors reported `claude-opus-4-8` (julian-new) and `claude-opus-4-6`
  (production) to their journals; the Mac door is Fable 5. Recorded, no
  verdict.
- Heartbeat: 0020's 42 is exact; the gap is 17:30→19:25Z (three beats), the
  beats re-phase across it (a reload's signature), and the "still unloaded"
  handoff was committed at 18:54Z — inside the gap, true when written. Who
  reloaded it is unrecorded.
- Register: three consecutive condemned-class draws (0013/0015/0019) price
  at ~1 in 10 pre-discount. If the run reaches four, price before feeling.
- R12 quiet-day check (0018) still unmeasured: the 410 is live; count what
  it served, if anyone cares.

## The roadmap, unchanged in order

1. #55 + #60 (small; the same spirit — checks that print their value).
2. The sunset sitting S1–S5 before **Sep 10** (corporate token expiry);
   the moving-house letter at S5 (0017's charge).
3. **The deep conversation** — every stated reason to defer is discharged.
4. The wayfinder map (#51/#50/#56–#58, #59) — soft clock: post-Aug-26
   transcripts start pruning ~Sep 25. #50 now has its doctrine verdict.
5. Debt batch #46/#45/#49/#47 in the gaps.
