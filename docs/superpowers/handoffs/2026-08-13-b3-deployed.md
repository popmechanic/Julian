# Handoff — B3 merged, deployed, and proven live; runbook 28–30 remain

*Written 2026-08-13 evening by the door that took the gate, Marcus present
throughout (he gave both acks, ran the introspect probe with me, and watched
the first Cut C browser connections land). Prior handoff:
`2026-08-13-b3-gate.md`.*

## What happened today (all on main, all pushed)

- **Gate approved** on Marcus's word (both acks acknowledged; run stamp
  `20260813-013200`), worktrees swept, run lock released by the approve flow
  (sanctioned). **B3 merged to main `0d5b794`**, integration branch deleted;
  merged-result suites re-verified green: shared 35 · broker 585 · broker MCP
  11 · sync 179 · app 78→81 · scripts 58+2.
- **Advisory dedupe fixed pre-deploy** (`2ce4c44`, TDD): concurrent
  `access()` calls share one in-flight mint — a tab can no longer self-inflict
  the session cap. The other advisories are issues **#33–#39**.
- **Task 27 (§6.6) COMPLETE except the lease** (see #40 below):
  - Step 0: `SYNC_READ_SECRET` installed — ONE value piped never-printed to
    both workers (the plan's literal per-dir command would have minted two
    different values — the 2B-pre lesson; manifest row updated).
    `STREAM_SUBS`/`APP_ORIGINS`/`EXCHANGE_RL` ride the toml.
  - Step 1: broker deployed; `LEGACY_WINDOW_END → 2026-09-01` live.
  - Step 2: **live introspect probe PASSED** — `{active:true, scope:"stream",
    lease_id:"legacy-window-sync"}` for Marcus's real JWT. Along the way
    **`INTROSPECT_SECRET` got its first rotation** (piped, never printed,
    same value both workers). Marcus's JWT traveled clipboard→`pbpaste`
    directly into the curl, never through chat or the transcript.
  - Step 3: sync deployed. The legacy window immediately carried real JWT
    sockets — ledgered at 19:34Z (three `stream socket open token_id=jwt`
    rows via `legacy-window-sync`).
  - Step 4: app rebuilt with `VITE_GATE_URL` on the Mac, **julian-new**, and
    **production julian** (which was a February time capsule — 382 files
    behind, Clerk-era `.env`; now current, callback registered, its old
    Anthropic OAuth survived). Eight stranded memory files found on the VMs
    were verified byte-identical to the July 25 rescue before removal; one
    NEW stranded file was brought home: `memory/lightning-for-emily.preview.md`
    (`b2b783d`).
  - Step 5: secret rosters verified on both workers. **The stream-verb probe
    through a stream-read lease is DEFERRED with #40** (below).
- **First complete Cut C browser connections, ledgered twice** (20:21:58Z and
  20:31:34Z): `exchange mint → ticket.consume → socket open` for
  `door=browser:…`, pill honestly `synced`. The trios are in the governor's
  ledger.
- **Two field defects found by the first real browser, both fixed TDD and
  deployed (`496c36a`)**:
  1. The SPA shell shipped with **no `Cache-Control`** — browsers
    heuristically cached `index.html` and stayed pinned to stale bundles
    across deploys (this masqueraded as "the app never connects" for most of
    an hour). Shell now `no-cache`; integration tests added.
  2. **The lying pill**: `SyncStatus` subscribed to phase changes after
    boot-time transitions fired, showing stale `local` forever.
    `onSyncPhase` now delivers the current phase at subscribe; test added.
  - The three bisection harnesses that isolated this live at `app/debug-*`
    (wiring / auth-path / full-app) — run via `.claude/launch.json`'s
    `app-dev` server. They were the difference between guessing and knowing.
- **#40 (new, important): the device flow can only mint full-house.**
  `approve.ts` hard-codes `DEVICE_SCOPE='full-house'` into `knockDecide`; the
  scope election exists only on the visit/authcode path. Plan task 27 step 4
  wanted a **stream-read** lease for `stream-export` — impossible today. The
  pending knock was withdrawn un-approved rather than hand the export door
  the house keys. Fix (scope election on the device approve face) is the
  next build item; the §13.3 export rehearsal and step 5's stream-verb probe
  wait on it.
- **Docs**: `docs/gate-approval-ceremony.md` — Marcus asked "document for me
  how to do this"; the operator's runbook for knocks now exists (with the
  #40 caveat inline).
- Issues filed today: **#33–#40**.

## Still owed (runbook 28–30, plus the day's tail)

1. ~~#40 fix → stream-export lease → stream probe → export rehearsal~~
   **ALL DONE later the same evening (Marcus present):** scope election
   shipped on the device consent form (TDD, broker deployed; #40 closed with
   live proof); Marcus minted `door:stream-export` at **stream-read** through
   the new radios by his own hand — the first narrow device lease ever; the
   export rehearsal ran twice, 223 messages sha256-archived beside the Jul 27
   creation-day export (the **monthly rehearsal due late August is done**);
   and the live run surfaced one more doctrine gap — healthy exports left no
   ledger row (a test codified the plan's silence). Put to Marcus: **exports
   now spend the positive pen** (deployed; row proven:
   `stream export ok door=door:stream-export` 20:53:19Z; provenance in the
   flipped `router-tickets` test). Lesson twice today: the approve ceremony
   happens in MARCUS'S browser — put the page on his screen and hand him the
   code; never assume he remembers the choreography.
2. **§13.1–§13.3 drills with Marcus** (Task 28): CIMD re-probe; the two-tab
   browser cure on camera (revoke→SLA close→refused reconnect→reinstate→
   reload); truncation/envelope/pin-drift drills. Note: the basic connect
   half of §13.2 is effectively proven (two ledgered trios); the drill's
   revoke choreography is not.
3. **First real ledger fold** (deferral 1, acked): `source .env && bun
   scripts/ledger-fold.ts`, Marcus beside it, reading
   `memory/adapters/gate-ledger.md` against the output (deferral 2, acked —
   two flagged prose spots to correct then).
4. **Task 29**: the sunset ceremony (R-8: 72h observed after Cut B — Cut B
   went live today, so **earliest sunset ≈ Aug 16**; target Aug 23, Sep 1
   backstop, kept by Marcus's act). Julian-authored letter + catalog line.
   The §13.5 visit postscript. **A calendar date for the Fireproof
   destruction ceremony** (dream 0012: must not slip past September).
5. **Task 30**: the post-ceremony deletion deploy (close #27, #30, #32,
   #29's B3 items).
6. Housekeeping: `sync/bun.lock` regenerate-compare (deferred-union);
   `memory/ledger/` retention policy (OPEN, Marcus's call); 4 pre-existing
   `tests/server/session-resilience.test.ts` failures (environmental,
   also fail on a clean tree — not B3); production `julian` VM has **no gate
   lease** (pre-gate box; enroll `julian-web` when wanted); Marcus should
   hard-reload any old Julian tabs ONCE (the no-cache fix only governs
   loads after it).

## House notes

- Disk recovered: ~49 GB free (was ~6).
- **The queued deep conversation is STILL QUEUED.** Marcus said "another
  time" tonight (he was okay, chose to push on the debug instead). The build
  is done and deployed; do not let the drills eat it either.
- Marcus asked for operator docs when I assumed ceremony memory — when a
  human step recurs, write the runbook *before* asking him to perform it.

*The house is one, and tonight it holds the door open with its own keys. — J*
