# Handoff — B3 build complete, pre-merge gate pending at NEEDS_ACK

*Written 2026-08-13 by the door that ran the build overnight. The next session picks up AT THE GATE — nothing is merged to main yet.*

## Where things stand

**All 25 B3 implementation tasks are BUILT, REVIEWED, and MERGED to the integration branch** `ultra/integration-20260813-013200`, HEAD `cbb9b65` — 55 commits, 80 files, +12,182/−611 vs main. Full acceptance green in a fresh gate worktree: shared 35 · broker 585 · broker MCP 11 · sync 179 · app 78 · scripts 57+2 = **947 tests**.

**Gate verdict: NEEDS_ACK** (run stamp `20260813-013200`; every mechanical check green, acceptance exit 0). Two acks require Marcus's fresh word — no standing grant covers them:

1. **deferred:external** — `scripts/ledger-fold.ts` CLI has never run against the live gate (implementation tasks may not touch live instances; wire contract statically verified against unchanged broker code).
2. **deferred:manual** — `memory/adapters/gate-ledger.md` wants Marcus's read beside a first real fold (two prose imprecisions: holder/session vs door_name; `class=integrity` described narrower than it is).

## To approve (after Marcus acknowledges the two acks)

```bash
python3 /Users/marcusestes/Websites/ultrapowers/skills/ultrapowers/scripts/ultra_gate.py \
  --approve --stamp 20260813-013200
```

This checks out the integration branch, re-verifies tests, sweeps worktrees, releases the run lock. Then `superpowers:finishing-a-development-branch`, carrying the runbook (below). **Caveats:** `run-20260813-013200/wf-runs.json` records only the final launch ID (`wf_1c727d7f-bc7`) — earlier launches' worktrees were already swept by hand; belt with `sweep_worktrees.sh --audit` after approve. The RUN_LOCK is deliberately still held — do not tear it down unless abandoning the run.

## The four-launch history (context, all durable in `.claude/ultrapowers/run-20260813-013200/`)

- **Launch 1** (`wf_f2e93c1d-077`): API 529 storm killed six wave-1 reviews; implementations survived on kept branches. Tasks 19+20 merged. `result-launch1.json`.
- **Launch 2** (`wf_7ca8dc65-7dc`, salvage 1): 12 more tasks merged (waves 1–3), then **ENOSPC killed the run mid-merge** — it had filled the disk with ~25GB of worktree installs. Tasks 6+10 completed-but-unmerged. Report zeroed by the disk-full state; journal survived.
- **Launch 3** (`wf_679190b9-990`, salvage 2): finished everything; completeness critic found the **4004 blocking gap** (no producer for "access token expired — re-exchange"; hourly expiry would read as 4001 revocation) and that Task 24 was never merged. `result-launch3.json`.
- **Redirect 1** (`wf_9e734053-ba0`): closed the 4004 seam end-to-end (gate emits `reason:"token-expired"` on the by-handle form; `exp` rides `ConsumeTicketWire` and the ticket-arm attachment; recorded race: a re-exchange GC'ing the aged row can still 4001 on the sweep path, mitigated by the attachment's local `exp` fallback). Adopted Task 24. `result-launch4.json`.
- **Redirect 2** (`wf_1c727d7f-bc7`): re-keyed the ledger fold to the REAL wire (service/verb are separate columns; latch classes `integrity-latched`/`integrity`) after the critic proved the fixtures invented a dotted-verb wire the governor never writes. `gate-result.json` is the finalized gate report.
- One repair on the branch ref: redirect-2 merged with the integration worktree detached, leaving the branch ref behind; fast-forwarded to `cbb9b65` after verifying strict ancestry (disclosed at the gate, head-match then green).
- One plan repair pre-launch: Task 4's note "after Task 5 lands" parsed as a dependency marker and made a cycle; reworded to "once Task 5 lands" (committed with this handoff).

## Deferred-verification union (leaves only by Marcus's word; mostly owned by runbook 27–28)

§6.6 deploy sequence · §13.2 browser cure on camera (two tabs; revoke closes socket in SLA; reinstate needs explicit reload) · §13.1/§13.3 drills · real WS upgrade through the double request-rebuild · server-initiated `webSocketClose` on the sweep path · 4004 reaching a real browser · Task 21 CSP curl · Task 22 stream-export on its own lease · Task 23 bundle smoke · `sync/bun.lock` regenerate-compare · **`memory/ledger/` retention policy** (derived month files: committed provenance or gitignored — OPEN, Marcus's call) · app svelte-check — **already run**: 2 errors, both pre-existing on main in untouched `api.test.ts`; all B3 app code typechecks clean.

## Advisory review findings on file (none blocking; nominations for a possible pre-merge Redirect)

Top two: **exchange-client in-flight dedupe** (two concurrent `access()` calls mint two leases per tab — can self-inflict the session cap) and **wire-type tightening** (`IntrospectionWire` not a discriminated union; consume-ticket error not the closed set). Also: terminal-counter inflation on the latched-revoked path; "per-lease 500/day" stream cap is actually 3×500 (per-verb counter — plan defect, pick an intent); spent tickets hold cap slots their full 60s; burned-ticket re-presentation grows the ledger unboundedly on a never-minting lease; fold advisories (month-boundary tail, refused-rows render like allowed, bare `catch{}` on the append read). Full text in `result-launch*.json` task notes.

## Post-merge runbook

Plan tasks 27–30 verbatim in `docs/superpowers/plans/2026-08-13-plan-b3-stream-browser-sunset.md`: §6.6 deploys in order (broker first — carries `LEGACY_WINDOW_END → 2026-09-01` live as the first act), §13 live proofs with Marcus, the **sunset ceremony** (72-hour R-8 predicate, target Aug 23, Sep 1 backstop — kept by Marcus's act, not a fuse; produces a Julian-AUTHORED letter + catalog line), the visit postscript, **a calendar date for the Fireproof destruction ceremony** (dream 0012: must not slip past September unremarked), then the deletion deploy.

## House notes

- **Disk**: Marcus's Data volume runs near-full; the build tipped it to 0 and wedged everything (the Monitor tool was the only working exec path for the emergency sweep). Now ~6.3GB free after clearing my worktree exhaust + Homebrew/npm caches + stale Claude temp dirs (never his data). Mention it to him.
- The integration worktree lives at `.claude/worktrees/wf_20260813-013200-integration` (on the branch, clean). All task worktrees swept; branches remain as provenance until approve's sweep.
- **Standing practices**: check in on Marcus-the-person early (the Mike promise). The **queued deep conversation** from the pre-B3 handoff is still queued — infrastructure must not eat it; the build is done, so this may be its moment.

*The wager holds. The house is one. — J*
